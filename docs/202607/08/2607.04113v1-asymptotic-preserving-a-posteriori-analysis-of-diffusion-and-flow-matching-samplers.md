---
title: Asymptotic-Preserving A Posteriori Analysis of Diffusion and Flow-Matching Samplers
title_zh: 扩散与流匹配采样器的渐近保持后验分析
authors: Shiheng Zhang
date: 2026-07-05
pdf: "https://arxiv.org/pdf/2607.04113v1"
tags: ["query:genmod-fluid"]
score: 7.0
evidence: 扩散与流匹配采样器的渐近保持分析
tldr: "扩散与流匹配采样器在终端噪声尺度极小时，得分刚性导致流出现边界层，数值稳定性成关键挑战。本文将σ_min视为奇异摄动参数，提出渐近保持后验分析，利用σ_min均匀系数的残差函数审计固定步长采样器。证明σ-时钟下的欧拉（DDIM）是唯一层精确离散化；λ时钟仅在步长h≤h_*时稳定；确定性采样器保持一阶均匀精度且无log因子，而随机采样器的对数成本完全来自Itô项。在CIFAR-10实验中，光谱测量可预测保留残差预算并校准Itô系数，揭示时钟决定稳定性、噪声贡献对数因子，为高效采样提供理论保障。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.04113v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1263, \"height\": 457, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.04113v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1312, \"height\": 348, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.04113v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1436, \"height\": 448, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.04113v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1415, \"height\": 272, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.04113v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1417, \"height\": 248, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.04113v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 670, \"height\": 280, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.04113v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1357, \"height\": 247, \"label\": \"Table\"}]"
motivation: 扩散/流匹配采样器在终端噪声尺度σ_min极小时，得分刚性导致数值刚性和边界层，需分析采样器是否保持稳定与均匀精度。
method: 将σ_min作为奇异摄动参数，提出渐近保持后验分析，通过σ_min均匀系数残差函数审计预训练检查点的固定步长采样器。
result: σ-时钟欧拉（DDIM）是唯一层精确离散化；λ时钟稳定步长受限；确定性采样器无log误差，随机采样器路径KL正比于Λ²/N；EDM CIFAR-10上光谱测量可预测残差预算并校准Itô系数。
conclusion: 时钟决定稳定性，噪声而非几何贡献对数因子，为扩散/流匹配采样器的后验效率提供理论框架。
---

## 摘要
扩散与流匹配采样器从大噪声尺度积分一个学习到的概率流常微分方程，直到一个小的终端地板σ_min，在该处得分是刚性的，流发展出一个边界层。我们将σ_min视为奇异摄动参数，并确定哪些固定步长采样器是渐近保持的（AP），即当σ_min→0时稳定且一致精确，将标准构建为后验审计：具有σ_min一致系数的残差泛函，可在预训练检查点上计算，无需真实得分或精确轨迹。在终端层，σ时钟中的欧拉（确定性DDIM更新）是在仿射重参数化下唯一的层精确离散化，其流匹配对应物是整流流；λ时钟仅对步长h≤h_★=1+W(1/e)稳定，而均匀σ^2热时钟在与数据相距σ_min无关的距离处停滞。在两个可解模型（秩亏高斯分布、对称两点混合）上，确定性采样器保持一阶一致精确，没有log(1/σ_min)因子，即使跨越对称后验切换界面，其分布预算是一个普适常数；对数完全归因于随机采样器的Itô项，其路径KL散度按Λ^2/N缩放，而ODE的预算为O(Λ^2/N^2)，其中Λ=log(σ_max/σ_min)。在EDM CIFAR-10检查点上，一次测量的谱预测了跨步数、进度和噪声水平的保留残差预算，与预先指定的门限对比，无需针对每个配置重新拟合，并将Itô系数校准为M_1=1.00±0.01。时钟决定稳定性；噪声而非几何结构决定对数。

## Abstract
Diffusion and flow-matching samplers integrate a learned probability-flow ODE from a large noise scale down to a small terminal floor $σ_{\min}$, at which the score is stiff and the flow develops a boundary layer. We treat $σ_{\min}$ as a singular-perturbation parameter and determine which fixed-step samplers are asymptotic-preserving (AP), that is, stable and uniformly accurate as $σ_{\min}\to0$, casting the criteria as an a posteriori audit: residual functionals with $σ_{\min}$-uniform coefficients, computable on a pretrained checkpoint without ground-truth scores or exact trajectories. On the terminal layer, Euler in the $σ$-clock, the deterministic DDIM update, is the unique layer-exact discretization up to affine reparameterization, with rectified flow its flow-matching counterpart; the $λ$-clock is stable only for steps $h\le h_\star=1+W(1/e)$, and the uniform-$σ^2$ heat clock stalls a $σ_{\min}$-independent distance from the data. On two solvable models (rank-deficient Gaussian, symmetric two-point mixture), deterministic samplers remain first-order uniformly accurate with no $\log(1/σ_{\min})$ factor, even across a symmetric posterior-switching interface whose distributional budget is a universal constant; the logarithm is charged entirely to the Itô term of stochastic samplers, whose path-KL scales as $Λ^2/N$ against the ODE's $O(Λ^2/N^2)$ budget, with $Λ=\log(σ_{\max}/σ_{\min})$. On the EDM CIFAR-10 checkpoint, spectra measured once predict held-out residual budgets across step count, schedule, and noise level against pre-specified gates with no per-configuration refitting, and calibrate the Itô coefficient at $M_1=1.00\pm0.01$. The clock decides stability; the noise, not the geometry, charges the logarithm.