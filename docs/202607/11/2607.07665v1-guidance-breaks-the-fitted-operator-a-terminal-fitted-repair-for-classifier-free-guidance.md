---
title: "Guidance Breaks the Fitted Operator: A Terminal-Fitted Repair for Classifier-Free Guidance"
title_zh: 指导打破拟合算子：一种终端拟合修复方法用于无分类器指导
authors: Shiheng Zhang
date: 2026-07-08
pdf: "https://arxiv.org/pdf/2607.07665v1"
tags: ["query:genmod-fluid"]
score: 8.0
evidence: 分析了扩散和流匹配采样器中无分类器指导的渐近保持性质
tldr: 针对无分类器引导(CFG)在高引导强度下的过饱和与不稳定问题，本文通过渐进保持数值分析揭示DDIM步骤在引导下不再拟合终端层，产生sigma_min发散残差。提出一个零额外NFE的修复：将CFG的方向权重从w(r-1)替换为r^(1+w)-r，消除了发散并实现一阶精度。在CIFAR-10和Stable Diffusion 1.5上，该修复在无额外成本下稳定高引导，9/9网格点FID优于CFG，但并非通用质量提升。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.07665v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 844, \"height\": 351, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.07665v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 637, \"height\": 382, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.07665v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 626, \"height\": 516, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.07665v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1054, \"height\": 376, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.07665v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1118, \"height\": 456, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.07665v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1255, \"height\": 564, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.07665v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1010, \"height\": 333, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.07665v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1232, \"height\": 166, \"label\": \"Table\"}]"
motivation: CFG在高引导时出现过饱和和不稳定，现有方法依赖更多步数或受限调度，缺乏对数值根源的分析。
method: 通过数值分析证明DDIM在引导下不再拟合终端层，提出修复：将CFG方向权重改为r^(1+w)-r，消除sigma_min发散。
result: 在CIFAR-10和Stable Diffusion 1.5上，修复方法在9/9网格点FID优于CFG，降低残差放大和饱和，但非通用质量提升。
conclusion: 该修复作为高引导稳定器有效，零额外NFE，但在密集CFG参考下并非更优积分器，有其应用边界。
---

## 摘要
无分类器指导（CFG）是增强扩散和流匹配采样器中类别条件化的标准方法，但在高指导强度下会出现过饱和和不稳定现象，从业者通过增加步数或限制区间调度来抑制这些症状。我们通过渐近保持的数值分析视角分析CFG。基于最近的结果，即确定性DDIM步骤是无指导终端层的唯一拟合算子，在采样的最后小西格玛段精确成立，我们证明指导恰好将判别子空间重新刚化到异常指数1+w。因此，DDIM在那里不再拟合，并且在粗网格上，其指导残差随着西格玛最小值趋近于零而发散。我们证明了一个具有三个有序步长阈值的指导时钟障碍，并将一步过饱和解释为其终点：这是校准模型上的求解器伪影，而非连续指导规律。同样的分析得出一个单系数、零额外NFE的修复方法：在指导方向上用r^(1+w)-r替换CFG的w(r-1)。在校准模型的判别交叉点上，这消除了CFG的西格玛最小值发散爆炸，并且当西格玛最小值趋近于零时，对精确指导流具有一阶精度。在学习的CIFAR-10检查点上，以及在Stable Diffusion 1.5 DDIM上的跨领域烟雾测试中，它作为一种高指导稳定器，无需额外成本，而非通用质量旋钮：它减少了残差放大和饱和，在测试网格上相对于CFG获得了9/9点FID胜利，并在硬细胞块中保持了分类器代理目标精度。我们还报告了其局限性：它不是通用图像质量提升，并且相对于密集的香草CFG参考，它不是该领域的统一更好的积分器。

## Abstract
Classifier-free guidance (CFG) is the standard way to strengthen class-conditioning in diffusion and flow-matching samplers, yet at large guidance it oversaturates and destabilizes, symptoms practitioners suppress with more steps or limited-interval schedules. We analyze CFG through an asymptotic-preserving, numerical-analysis lens. Building on a recent result that the deterministic DDIM step is the unique fitted operator for the unguided terminal layer, exact on the final small-sigma stretch of sampling, we show that guidance re-stiffens exactly the discriminative subspace to an anomalous exponent 1+w. DDIM is therefore no longer fitted there, and on coarse meshes its guided residual diverges as sigma_min goes to zero. We prove a guided clock barrier with three ordered step-size thresholds, and read one-step oversaturation as its endpoint: a solver artifact on the calibration model rather than the continuous guided law. The same analysis yields a one-coefficient, zero-extra-NFE repair: replace CFG's w(r-1) by r^(1+w)-r on the guidance direction. On the calibration model's discriminative crossover, this removes CFG's sigma_min-divergent blow-up and is first-order accurate against the exact guided flow as sigma_min goes to zero. On learned CIFAR-10 checkpoints, and as a cross-domain smoke test on Stable Diffusion 1.5 DDIM, it acts as a high-guidance stabilizer at no extra cost rather than a universal quality knob: it cuts residual amplification and saturation, gives 9/9 point-FID wins over CFG on the tested grid, and preserves classifier-proxy target accuracy in the hard-cell blocks. We report the limits alongside: it is not a universal image-quality win, and against a dense vanilla-CFG reference it is not a uniformly better integrator of that field.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：在扩散模型和流匹配采样器中，无分类器指导（CFG）是增强类别条件化的标准方法。然而，当指导强度（guidance scale）增大时，CFG会出现过饱和和不稳定现象。现有解决方法（如增加采样步数、使用有限区间调度）只是缓解症状，并未深入理解其数值根源。
- **核心问题**：本文从渐近保持（asymptotic-preserving，AP）的数值分析视角出发，研究DDIM步骤在CFG下的行为。关键发现：确定性DDIM步骤在无指导终端层是唯一拟合算子（fitted operator），但在CFG作用下，由于引导方向重新刚化判别子空间至异常指数 \(1+w\)，DDIM不再拟合，导致其残差在粗网格上随 \(\sigma_{\min}\to 0\) 发散。这种发散是求解器伪影（solver artifact），而非连续引导律本身的问题。
- **整体含义**：论文揭示了CFG高引导不稳定的数值根源，并提出一个零额外NFE的修复系数，从终端拟合角度解决了 \(\sigma_{\min}\)-发散问题，同时保持一致性。

## 2. 论文提出的方法论：核心思想、关键技术细节

### 核心思想
- 将CFG视为在条件流上增加一个引导方向 \(\gamma = \eta_c - \eta_u\)，其权重为 \(w\)。
- 分析发现：在判别子空间（类内方差为0、边缘方差非零的方向）上，引导流的终端指数从1变为 \(1+w\)，使得原始DDIM步骤（对应指数1）不再拟合，导致反射、残差放大和绝对稳定性问题。
- 通过量规变换 \(\tilde{\xi} = \sigma^{-w}\xi\) 将异常指数 \(1+w\) 修正为1，再应用σ-clock欧拉步，最终推导出修正系数。

### 关键技术细节
- **模型假设**：在类-边缘可交换高斯模型（假设类协方差子集于边缘协方差）上进行分析，将状态空间方向分为三类：
  - 共享正态方向：指数保持1；
  - 切向方向：指数冻结为0；
  - 判别正态方向：指数变为 \(1+w\)。
- **指导时钟障碍（Theorem 1）**：在纯判别层上，DDIM+CFG的放大因子为 \(|G_w|e^h\)，其中 \(G_w = (1+w)e^{-h} - w\)。存在三个有序阈值：
  - \(h_{\flat} = \log(1+1/w)\)：超过此阈值每一反射坐标。
  - \(h_{\sharp} = \log(1+2/w)\)：超过此阈值残差开始指数增长。
  - \(h_{\infty}\)（仅当 \(w>1\)）：超过此阈值失去绝对稳定性。
- **修复公式（Definition 1）**：
  \[
  x_{n+1} = D_c + r(x_n - D_c) + (r^{1+w} - r)(D_u - D_c)
  \]
  其中 \(r = \sigma_{n+1}/\sigma_n\)。该系数是唯一不依赖谱信息的终端精确系数（Lemma 2），保持坐标符号（Proposition 3），且在 \(r\to0\) 时收敛到条件去噪器 \(D_c\)（而CFG收敛到 \(D_w\)，即过饱和的机制）。
- **精度保证**：在判别交叉上，修复步骤对精确引导流是一阶一致、渐近保持（在 \(\sigma_{\min}\) 意义上），常数不依赖于 \(\sigma_{\min}\) 和步数（Theorem 2），但不统一于 \(w\)。

## 3. 实验设计

### 数据集/场景
- **合成数据**：单坐标高斯模型（\(a=0<b\)），验证残差放大和修复效果。
- **真实模型**：NVIDIA EDM CIFAR-10 VP检查点（类条件和无条件分别训练）。
- **跨领域烟雾测试**：Stable Diffusion 1.5 DDIM（潜在扩散模型）。

### Benchmark
- **残差诊断**：放大中位数（amp med）、95百分位放大（amp p95）、裁剪分数中位数（clip ∆）。
- **图像质量**：FID、KID、分类准确率（目标标签）。
- 对比方法：CFG（原始）、修复后的CFG（fitted CFG）、终端间隔基线（vanilla CFG在满足 \(h>h_\flat\) 时关闭引导，即退化为无条件DDIM），以及调优的有限间隔基线。

### 对比方法
- 主要与标准CFG对比，同时与终端间隔、调优间隔基线比较。

## 4. 资源与算力

文中未明确说明使用的GPU型号、数量、训练时长。作者提到所有实证诊断使用公开检查点、自包含脚本，且脚本有CPU自检模式。合成交叉验证无需检查点。因此算力信息缺失，仅从复现角度可知计算量较小（检查点仅推理），但无具体数值。

## 5. 实验数量与充分性

### 实验数量
- **合成交叉验证**：一个图（Figure 2），展示不同步数下残差放大随 \(\sigma_{\min}\) 变化。
- **真实检查点残差诊断**（Table 1）：两个类别（class 0, class 1），\(w\in\{4,6.5,8\}\)，\(N\in\{8,16,32\}\)，共 \(2\times3\times3=18\) 个细胞，每个64种子。
- **图像质量评估**（Table 2）：硬细胞 \(w=8,N=8\)，两个5k块和一个50k复制，共3个块，每个报告FID/KID/amp p95/clip p95/target acc。
- **稳健性网格**：9个细胞（\(w\in\{4,6.5,8\}\), \(N\in\{8,16,32\}\)），每个5k图像。
- **间隔基线比较**（Table 3）：3个细胞（\(w=8,N=8\); \(w=8,N=16\); \(w=6.5,N=8\)），每个5k图像。
- **所有类别审计**：10个类别 × 6个高指导细胞 = 60个类别-细胞对。
- **潜在扩散烟雾测试**（Table 4）：两种设置（g=12,N=12 256图；g=7.5,N=20 128图）。
- **密集参考诊断**（Table 5）：2个细胞（\(w=8,N=8/16\)），512参考步。
- **DINOv2特征审计**：2个5k块，计算FD-DINOv2和MMD。

### 充分性与公平性
- **充分性**：覆盖了合成模型、真实模型、跨领域转移；残差和图像质量均有评估；包含多个随机种子和复制；进行了消融（间隔基线、调优间隔）。作者也报告了指标分歧（FID vs KID）和局限性。
- **客观性**：所有实验均固定种子和网格，方案预先指定。指标使用标准FID/KID。但50k复制仅在单个硬细胞进行，九网格仅为5k，规模有限。KID分叉未被完全解释，可能存在偏差。此外，模型假设（类子集）在真实数据中未必成立，但实验中使用了两个独立检查点（类条件和无条件），部分缓解了假设依赖。

## 6. 论文的主要结论与发现

- **核心发现**：CFG通过重新刚化判别子空间至指数 \(1+w\) 破坏了DDIM的拟合算子性质，导致在粗网格上出现 \(\sigma_{\min}\)-发散残差，这是高引导过饱和的数值根源。
- **修复的有效性**：提出的单系数修复（\(r^{1+w}-r\)）在零额外NFE下消除了CFG的发散性和反射，保持一阶精度，并在CIFAR-10上9/9网格点FID优于CFG，显著改善残差放大和裁剪，同时保持分类器目标精度。
- **跨领域可迁移**：在Stable Diffusion 1.5上作为烟雾测试，该修复大幅降低像素饱和（p95从0.134降至0.009），同时CLIP文本对齐损失很小，优于终端引导关闭。
- **局限性**：
  - KID在大多数情况下仍然偏向CFG，表明修复并非通用图像质量提升。
  - 相对于密集CFG参考（512步），修复的最终图像L2并不更接近，说明它不是该领域的统一更好积分器。
  - 当类方差大于边缘方差时（反序），连续引导流自身不稳定，任何求解器都无法修复。

## 7. 优点

- **理论驱动**：从渐近保持数值分析出发，严格推导出修复系数，提供了过饱和的纯求解器根源解释。
- **零额外计算成本**：仅改变一个系数，无需额外网络评估，即插即用。
- **简单而有效**：修复公式简洁，与现有代码可无缝集成。
- **跨领域验证**：从像素空间CIFAR-10到潜在空间Stable Diffusion均表现一致。
- **完整的理论保证**：在模型问题上，证明了消除 \(\sigma_{\min}\)-发散、一阶精度、符号保持等性质。

## 8. 不足与局限

- **实验规模有限**：50k复制仅在单个硬细胞进行，九网格仅5k样本，KID分叉未在大规模上验证。
- **指标分歧未解决**：FID和KID偏好不一致，论文指出修复降低对比度，但未提供因果分析证明修复优于CFG。
- **依赖模型假设**：类子集假设（类协方差窄于边缘）在真实数据中未必成立，反序方向下引导流自身不稳定。
- **不适用于密集CFG参考**：相对于512步CFG参考，修复的最终图像L2更大，说明它不是该场的更好积分器。
- **在弱指导下的优势不明显**：中位数放大在 \(w=4\) 时混合，仅在尾部和裁剪有改进。
- **未与其他现代修复方法全面比较**：如CFG++、APG、CFG-Zero*、C2FG等，仅在论文讨论部分提及，未做实验对比。
- **算力和复现细节不详**：缺少GPU型号、运行时间等信息，可能影响可复现性。

（完）
