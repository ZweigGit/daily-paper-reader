---
title: "Beyond Trajectory Matching: Reflow with Marginal Distribution Alignment"
title_zh: 超越轨迹匹配：基于边缘分布对齐的回流方法
authors: "Chen Wang, Peiran Yun, Pan Xie, Ke Deng"
date: 2026-06-28
pdf: "https://arxiv.org/pdf/2606.29287v1"
tags: ["query:genmod-fluid"]
score: 8.0
evidence: 提出改进的基于reflow的蒸馏方法用于流匹配，可直接应用于流体流动模拟
tldr: 现有reflow蒸馏通过轨迹匹配加速扩散模型，但存在理论缺陷：轨迹匹配不能唯一确定学生模型诱导的分布，导致生成质量差异。本文提出边缘对齐正则化，通过跟踪学生ODE的对数密度变化，利用冻结教师模型的分数计算惩罚项，强制学生末端分布与教师分布一致。理论证明局部边缘对齐控制最终分布差异的总变差界。实验表明该方法在少步生成中显著提升质量，无需额外网络或对抗优化。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.29287v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1442, \"height\": 593, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.29287v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1445, \"height\": 317, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.29287v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 924, \"height\": 1268, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.29287v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1236, \"height\": 311, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.29287v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 618, \"height\": 548, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.29287v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 761, \"height\": 444, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.29287v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1007, \"height\": 459, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.29287v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 724, \"height\": 169, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.29287v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1045, \"height\": 166, \"label\": \"Table\"}]"
motivation: 现有reflow蒸馏中轨迹匹配不能唯一确定学生分布，导致生成质量差异。
method: 提出边缘对齐正则化，通过跟踪学生ODE对数密度变化并利用冻结教师分数惩罚分布差异。
result: 在多个基准上验证，有效提升少步生成质量，且无需额外训练网络或对抗优化。
conclusion: 边缘对齐正则化可统一应用于reflow家族，理论证明了分布差异控制的有效性。
---

## 摘要
扩散和连续流生成模型实现了高质量的生成，其确定性采样可被形式化为求解学习到的ODE动力学。然而，准确的ODE离散化通常需要多个步骤，这使得高效的少步生成成为关键挑战。在加速策略中，基于回流的蒸馏简化了教师ODE轨迹，使得学生模型能够用更少的步骤逼近教师传输。我们识别出该范式的一个理论局限性，即轨迹匹配可能无法充分确定学生模型诱导的分布。具体而言，两个学生模型可能达到相同的轨迹匹配损失，但诱导出不同的终点边缘分布，从而导致不同的生成质量。为解决这一局限性，我们引入了一种边缘对齐正则化项，用于惩罚在每个蒸馏区间终点处学生诱导的边缘分布与对应教师边缘分布之间的差异。该正则化项通过跟踪学生模型诱导的ODE沿线的对数密度变化，并评估冻结的教师模型中的得分来计算，无需辅助可训练网络或对抗优化。所提出的框架统一适用于回流系列，包括原始回流和分段回流。我们进一步证明了一个伸缩总变分界，表明局部边缘对齐控制了学生诱导分布与教师诱导分布之间的最终时间差异。在基准主干上的实验证明了所提方法在少步生成中的有效性。

## Abstract
Diffusion and continuous-flow generative models achieve high-quality generation, and their deterministic sampling can be formulated as solving learned ODE dynamics. However, accurate ODE discretization often requires many steps, making efficient few-step generation a key challenge. Among acceleration strategies, reflow-based distillation simplifies teacher ODE trajectories so that a student model can approximate the teacher transport with fewer steps. We identify a theoretical limitation of this paradigm, namely that trajectory matching can under-determine the distribution induced by the student model. In particular, two student models can attain the same trajectory-matching loss while inducing different endpoint marginal distributions, which may lead to different generation quality. To address this limitation, we introduce a marginal-alignment regularizer that penalizes the discrepancy between the student-induced marginal and the corresponding teacher marginal at the endpoint of each distillation interval. The regularizer is computed by tracking log-density changes along the ODE induced by the student model and evaluating scores from the frozen teacher model, without requiring auxiliary trainable networks or adversarial optimization. The resulting framework applies uniformly to the reflow family, including vanilla reflow and piecewise reflow. We further prove a telescoping total-variation bound showing that local marginal alignment controls the final-time discrepancy between the student-induced and teacher-induced distributions. Experiments on benchmark backbones demonstrate the effectiveness of the proposed method for few-step generation.

---

## 论文详细总结（自动生成）

好的，以下是根据您提供的论文《Beyond Trajectory Matching: Reflow with Marginal Distribution Alignment》内容生成的结构化中文总结。

# 论文深度总结：《超越轨迹匹配：基于边缘分布对齐的回流方法》

## 1. 核心问题与研究动机

*   **核心问题**：在扩散和连续流生成模型的加速蒸馏方法中，传统的 **“轨迹匹配”（Trajectory Matching）** 范式存在一个被忽视的理论缺陷，即**仅优化轨迹匹配损失无法唯一确定学生模型生成的最终数据分布**。
*   **研究背景**：为了提高扩散/流模型的采样效率，Reflow（回流）类蒸馏方法通过让学生模型模仿教师模型的简化轨迹，从而实现少步采样。然而，现有方法（如PeRFlow）仅依赖轨迹级别的L2损失进行监督。
*   **关键发现（动机驱动）**：作者通过理论证明（Theorem 1），两个拥有完全相同轨迹匹配损失的学生模型可以产生截然不同的终点边缘分布，从而导致生成质量的显著差异。这意味着**轨迹匹配对分布的控制力是不足的**，需要一个额外的分布级信号来补充。

## 2. 方法论：边缘对齐正则化

*   **核心思想**：在传统的轨迹匹配损失（\( L_{TM} \)）的基础上，增加一个**边缘对齐正则化项（\( L_{MA} \)）**。该正则项直接强制学生模型在每个蒸馏子区间（time window）的终点处的**边缘分布**与教师模型在该点的边缘分布保持一致。
*   **关键技术细节**：
    1.  **损失函数**：总损失为 \( L_{total}(\theta) = L_{TM}(\theta) + \lambda L_{MA}(\theta) \)。
        *   \( L_{TM}(\theta) \)：标准的PeRFlow轨迹匹配损失，确保学生速度场与教师轨迹上的直线路径对齐。
        *   \( L_{MA}(\theta) \)：边缘对齐损失，定义为学生模型在区间终点 \( t_k \) 处的分布 \( \tilde{p}_{\theta}^{(k)} \) 与教师模型在该点的边缘分布 \( q_{\psi, t_k} \) 之间的Kullback-Leibler散度（KL散度），即 \( KL(\tilde{p}_{\theta}^{(k)} || q_{\psi, t_k}) \)。
    2.  **计算技巧（关键创新）**：
        *   **避免额外网络**：\( L_{MA} \) 的计算不需要训练额外的判别器或学生分数网络。
        *   **Liouville表示**：将KL散度转化为一个期望形式，其梯度可以通过一个“增强ODE求解”得到。该过程同时追踪状态 \( x \) 的演化以及**状态变化引起的对数密度变化（\( \Delta_{Stu} \)）**。
        *   **梯度公式**：梯度的计算只依赖于学生模型速度场 \( \vec{f}_\theta \) 的散度（通过Hutchinson迹估计器高效计算）和**冻结的教师模型的分数函数 \( s_\psi \)**。
        *   **算法流程（以PeRFlow为例）**：
            1.  采样数据X1和噪声X0。
            2.  计算教师端点Y (DDIM采样)。
            3.  计算标准的PeRFlow损失（Step 8）。
            4.  每隔m次迭代，执行一次“增强ODE求解”，从区间起点X积分到终点，同时得到终点状态 \( X_{Stu} \) 和累积对数密度变化 \( \Delta_{Stu} \) （Step 10）。
            5.  利用冻结教师分数 \( s_\psi \) 计算边缘对齐损失 \( \ell_{MA} \) （Step 11）。
            6.  合并损失并更新梯度。
*   **理论保证**：论文的**Theorem 2**提供了一个**伸缩总变分（TV）界**，证明了最小化局部（每个区间）的边缘对齐损失可以有效地约束学生最终分布与教师最终分布之间的总变分差距。

## 3. 实验设计

*   **数据集**：
    *   主实验：LAION-Aesthetics v2 5+ 数据集用于训练。评估在 **MS-COCO 2014** 和 **MS-COCO 2017** 验证集上进行。
    *   补充实验：CelebA-HQ 256×256 数据集。
*   **Benchmark（评价基准）**：
    *   主干网络：**Stable Diffusion v1.5 (SD 1.5)** 和 **Stable Diffusion XL (SDXL)**。
    *   评价指标：**FID** (Fréchet Inception Distance，衡量图像质量与多样性) 和 **CLIP Score** (衡量文本-图像对齐程度)。
*   **对比方法**：
    *   **基线**：原始的 PeRFlow。
    *   **其他蒸馏方法**：LCM-LoRA, 2-ReFlow, Flash Diffusion, PCM, SDXL-Lightning, HyperSD, LCM, DMD2, Rectified Diffusion 等。
    *   **训练-free求解器**：DPMSolver, DPMSolver++, DDIM。

## 4. 资源与算力

*   论文明确说明了计算资源：
    *   **硬件**：2 × NVIDIA H200 GPUs。
    *   **精度**：BF16 混合精度训练。
    *   **训练时长**：在SD 1.5上进行5K步微调需要大约**1天**。
    *   文中也提到，由于需要每隔几步进行一次增强ODE求解（计算 \( L_{MA} \)），这大约增加了**30%** 的总训练时间。

## 5. 实验数量与充分性

*   **实验数量**：
    *   **主干网络**：2个（SD 1.5, SDXL），覆盖了最主流的文生图模型。
    *   **数据集**：主要在COCO上进行评估，辅以CelebA-HQ，覆盖了不同领域（自然图像、人脸）。
    *   **对比方法**：对比了7种以上主流的蒸馏/加速方法，包括基于一致性、对抗性和分布匹配的多种流派。
    *   **消融/敏感性分析**：
        *   对**CFG（无分类器引导）尺度**进行了敏感性分析（2-7）。
        *   对**正则化权重** \( \lambda \) 进行了敏感性分析（10⁻⁷ 到 10⁻⁵）。
    *   **不同初始化条件**：在CelebA-HQ的实验中，分别测试了从教师模型初始化和从已收敛的PeRFlow模型初始化两种设置。
*   **充分性与客观性**：
    *   **充分**：实验设计全面，涵盖了主流的架构和对比方法，并进行了必要的超参数敏感性分析。
    *   **公平**：在各自官方或推荐设置下进行比较，且SD 1.5和SDXL的基线（PeRFlow）使用官方公开的检查点。论文也指出了在更广泛设置下的验证留作未来工作，态度坦诚。
    *   **客观**：所有评估结果以表格形式清晰呈现，并提供了定量改善的指标。

## 6. 主要结论与发现

*   **理论发现**：严谨地证明并指出了Reflow家族中轨迹匹配损失的理论局限性：**无法唯一确定学生模型诱导的分布**。
*   **方法有效性**：提出的**边缘对齐正则化**是解决该局限性的有效手段，能够在不增加额外可训练网络的情况下，显著提升少步生成的质量。
*   **实验验证**：在SD 1.5和SDXL等主流模型上，该方法一致性地超越了PeRFlow基线，并在多项FID和CLIP分数指标上达到或接近了当时最优（SOTA）的4步蒸馏方法。
*   **兼容性**：该正则化项与标准的CFG推断完全兼容，不需要特殊调优。

## 7. 优点

*   **理论性强**：论文不仅有实验，还提供了坚实的理论分析，指出了现有范式的根本问题，并提供了新方法效果的保证（Theorem 2的TV界）。
*   **方法轻量优雅**：无需引入任何额外的可训练网络（如判别器、学生分数网络）或对抗训练，仅通过冻结的教师分数和一个增强ODE求解即可实现。这使得训练更加稳定、简洁。
*   **即插即用**：该方法可以**统一应用于整个Reflow家族**（包括原始Reflow和PeRFlow），作为对现有训练的“补丁”，兼容性好。
*   **性能提升显著**：在多个基准测试上，相比基线（PeRFlow），在相同的少步推断预算下实现了明确的FID和CLIP分数提升。

## 8. 不足与局限

*   **实验覆盖范围有限**：文中承认，虽然该方法理论上适用于不同噪声调度和 \( K=1 \) 的设置（原始Reflow），但**这些扩展的实证评估因计算限制而未进行**。
*   **不适用于非ODE学生模型**：论文明确指出，该框架依赖于ODE动力学来定义和计算 \( L_{MA} \)。因此，对于另一大类学生模型，即**一致性模型（Consistency Models）** 或**捷径模型（Shortcut Models）**，该方法不直接适用。
*   **计算开销**：尽管没有引入额外的网络，但计算边缘对齐损失需要进行一个增强的ODE求解，这会导致大约30%的训练时间增加，对于追求极致训练效率的场景可能是个小缺点。
*   **应用风险**：作为生成模型加速技术，可能会降低生成误导信息或非自愿图像的门槛，这是一个需要关注的广泛社会风险。论文也提到了这一点。

（完）
