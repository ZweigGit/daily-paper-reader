---
title: Incomplete Observations Boost Evolutionary Performance in Ocean Modeling
title_zh: 不完整观测提升海洋建模的演化性能
authors: "Yangyang Kong, Yutong Jiang, Yanhai Gan, Junyu Dong, Feng Gao, Xiaopei Lin"
date: 2026-07-21
pdf: "https://arxiv.org/pdf/2607.19147v1"
tags: ["query:genmod-fluid"]
score: 8.0
evidence: 用于海洋建模的生成式状态空间模型，从稀疏观测学习
tldr: 现有海洋数据驱动模型依赖完整再分析数据，导致计算瓶颈和性能受限。本文提出生成式状态空间模型与EM优化框架，直接从稀疏噪声观测中学习海洋动态。该方法将海洋物理量作为隐状态，采用神经网络建模演化，并通过Langevin动力学重建高保真场。在CMIP6和卫星数据上验证了高保真重建与预测能力，表明稀疏观测可直接提升模型表现。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.19147v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1457, \"height\": 641, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.19147v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1212, \"height\": 1641, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.19147v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1284, \"height\": 1577, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.19147v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1294, \"height\": 2010, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.19147v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1244, \"height\": 303, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.19147v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1088, \"height\": 195, \"label\": \"Table\"}]"
motivation: 当前海洋建模依赖完整再分析数据，成本高且性能受限于训练数据质量，亟需从稀疏真实观测中学习的可扩展方法。
method: 构建隐马尔可夫模型，以神经网络参数化状态转移和初始状态，发射模块用掩码高斯分布，结合EM算法与Langevin动力学交替优化。
result: 在CMIP6模拟与FY-3D卫星数据上实现高保真重建与准确预测，稀疏观测有效改善模型对海洋动力学的表征。
conclusion: 该框架为下一代地球系统模型提供了从稀疏、不完全观测直接学习的可扩展途径。
---

## 摘要
数据驱动方法已经彻底改变了海洋建模，但当前方法严重依赖于完整的再分析数据集，这不仅带来了计算约束，还将模型性能限制在训练数据的水平上。在这里，我们提出了一种生成式状态空间模型和一个优化框架，使得能够直接从稀疏且含噪声的观测中学习。该模型本质上是一个具有连续状态空间的隐马尔可夫模型，其中海洋物理量被视为隐藏状态，测量值作为观测，从而实现了海洋场和观测数据的统一表示。初始状态和状态转移模块均采用神经网络实现，以捕捉海洋状态的复杂性和时间演化，而发射模块则被表述为掩码高斯分布。为了从稀疏观测中训练模型，我们基于期望最大化（EM）算法推导了一个优化框架。该框架交替通过朗之万动力学重建高保真海洋场，并优化深度神经网络以捕捉时间演化。理论分析表明，该框架在生成模型下最大化了观测的似然性。为了提高效率，我们假设海洋状态演化遵循平稳、遍历且马尔可夫的随机过程，并在优化过程中仅采用长度为二的状态序列。在CMIP6模拟数据和FY-3D卫星数据上的实验展示了高保真重建和准确预测，表明稀疏观测可以直接改善模型对海洋状态动力学的表示。这项工作为下一代地球系统模型直接从稀疏、不完整的真实观测中学习提供了一条可扩展的路径。

## Abstract
Data-driven methods have revolutionized ocean modeling, yet current approaches rely heavily on complete reanalysis datasets, imposing computational constraints and limiting model performance to that of the training data. Here, we present a generative state-space model and an optimization framework that enable learning directly from sparse and noisy observations. The model is essentially a hidden Markov model with a continuous state space, where oceanic physical quantities are treated as hidden states and measurements as observations, enabling a unified representation of ocean fields and observational data. Both the initial-state and state-transition modules are implemented as neural networks to capture the complexity and temporal evolution of ocean states, while the emission module is formulated as a masked Gaussian distribution. To train the model from sparse observations, we derive an optimization framework based on the expectation-maximization (EM) algorithm. The framework alternately reconstructs high-fidelity ocean fields via Langevin dynamics and optimizes deep neural networks to capture temporal evolution. Theoretical analysis shows that the framework maximizes the likelihood of observations under the generative model. For efficiency, we assume that ocean-state evolution follows a stationary, ergodic, and Markovian stochastic process and adopt only length-two state sequences during optimization. Experiments on CMIP6 simulation data and FY-3D satellite data demonstrate high-fidelity reconstruction and accurate prediction, showing that sparse observations can directly improve the model's representation of ocean-state dynamics. This work offers a scalable pathway for next-generation Earth system models to learn directly from sparse, incomplete real-world observations.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：当前最先进的AI海洋模型（如Pangu-Weather、GraphCast）严重依赖完整的再分析数据集（如ERA5、GLORYS）进行训练。然而，再分析数据的生成需要高昂的计算成本（数据同化），其时空分辨率受限，且性能受限于观测覆盖度和数值模型精度，导致AI模型只能充当“仿真器”而非独立“模拟器”。
- **核心问题**：能否让AI模型直接从稀疏、含噪声的真实观测（如卫星扫描数据）中学习海洋动力学，从而克服对完整再分析数据的依赖？
- **整体含义**：本研究提出一种生成式状态空间模型与期望最大化（EM）优化框架，旨在实现从稀疏观测中自举式学习，为下一代地球系统模型提供可扩展的、不依赖完整再分析数据的训练范式。

### 2. 论文提出的方法论

- **核心思想**：将海洋动力系统建模为隐马尔可夫模型（HMM），其中完整物理场作为隐状态（s），稀疏观测作为观测变量（o）。利用深度神经网络参数化初始状态分布和状态转移分布，并通过EM算法迭代求解最大似然估计，从而同时实现状态重建与模型学习。

- **关键技术细节**：
  - **初始状态模型**（`p_θ(s0)`）：基于改进的StyleGAN2-ADA架构，采用深度可分离卷积（DWConv+PWConv）实现轻量化，并设计了特殊的风格注入策略（“注入在DWConv，归一化在PWConv”）以解决数学抵消问题。
  - **状态转移模型**（`p_θ(s_{t+1}|s_t)`）：采用随机U-Net作为生成器，输入当前状态`st`和潜在噪声`z`，输出下一个状态。使用全局条件判别器（而非PatchGAN）以强制物理一致性。
  - **观测模型**：假设观测为来自完整物理场的稀疏子集并受高斯噪声污染，形式化为掩码高斯分布：`p(o_t|s_t) = N(o_t | M_t ⊙ s_t, σ²I)`。
  - **EM优化框架**（算法1）：
    - **E步**：在潜在空间（由生成模型定义的噪声向量`z`）上执行朗之万动力学采样，近似后验`p_θ(z|o)`，从而重建完整状态`s`。
    - **M步**：将E步重建的完整状态序列作为伪真值，分别优化初始状态模型和转移模型（使用不同对抗损失设计）。
    - **效率假设**：假定海洋状态演化为平稳、遍历、马尔可夫随机过程，实际仅采用长度2的状态序列（T=1）进行优化。

### 3. 实验设计

- **数据集与场景**：
  - **模拟数据（OSSE）**：基于CMIP6 BCC-CSM2-MR气候模型，取每日海表温度（SST）作为真值。通过结构掩码（约67%区域缺失）叠加高斯噪声生成稀疏观测。
  - **真实卫星数据**：FY-3D微波辐射成像仪（MWRI）北极区域（>66°N）Level-1扫描数据，观测间隔约1.72小时，存在天然沿轨数据空洞。

- **基准（Benchmark）**：
  - **预训练模型**：仅使用少量完整SST场（约800样本）进行标准监督学习获得的模型，作为基线。
  - **最终模型**：在预训练基础上，经过17次EM迭代（使用约1500个稀疏观测样本）优化后的模型。

- **对比方法**：仅与自身预训练模型对比，未与其他稀疏重建或数据同化方法（如传统插值、深度学习inpainting、变分同化等）进行横向比较。

- **评价指标**：均方根误差（RMSE，°C）、平均绝对误差（MAE，°C）、结构相似性指数（SSIM）。真实数据仅做定性分析。

### 4. 资源与算力

- 论文未明确说明使用的GPU型号、数量、训练时长等计算资源信息。仅在算法中提到使用Adam优化器，未提供详细的硬件配置或能耗数据。

### 5. 实验数量与充分性

- **实验组数**：主要包括三组实验：
  1. 模拟数据下的重建与预测定量对比（表1、表2），分别报告s0（初始状态重建）和s1（一步预测）的RMSE/MAE/SSIM。
  2. 模拟数据下的定性可视化对比（图3），展示了四种结构掩码下的重建效果。
  3. 真实卫星数据上的定性重建对比（图4），展示了三个场景下T0到T1的时空连续性。
- **消融/分析实验**：在方法设计中，论文通过对比实验确定了潜在空间采样时的三项有效策略（增加潜在自由度、使用Tanh激活函数、采用高斯先验噪声），这部分可视为消融研究。
- **充分性评估**：
  - **优点**：验证了EM迭代带来的显著提升，且在模拟和真实数据上均展示了效果。
  - **不足**：
    - 基准对比过于单一（仅与自身预训练模型对比），缺乏与现有稀疏数据重建方法（如OISST插值、GANs、扩散模型、变分同化等）的横向比较。
    - 真实数据部分缺少定量评价（无真值），仅靠视觉判断，说服力有限。
    - 仅测试了两帧（T=1）场景，未验证更长时序下的动态学习能力。
    - 未对掩码率（67%）进行敏感性分析，也未测试不同噪声水平的影响。

### 6. 论文的主要结论与发现

- **核心发现**：基于EM的迭代学习框架使得生成模型能够从稀疏、不完整的观测中自举式提升物理状态重建与预测性能。在模拟数据上，最终模型相比预训练模型将初始状态重建的RMSE降低28.13%，一步预测RMSE降低27.09%。
- **重要启示**：深度生成模型有能力通过利用自身的不完美重建来精炼对物理动力学的理解，从而从碎片化数据中提炼出连贯的物理规律。
- **实际应用**：在真实FY-3D卫星数据上，最终模型能恢复锐利的温度梯度和冰缘精细结构，并表现出更符合物理规律的时空演化，避免了简单插值方法产生的时序抖动。

### 7. 优点

- **方法论创新**：首次将EM算法与深度生成状态空间模型结合，用于从稀疏海洋观测中直接学习，打破了对完整再分析数据的依赖。
- **架构设计的实用改进**：针对StyleGAN2-ADA的轻量化改造（DWConv）以及风格注入策略，保证了生成质量的同时降低了计算复杂度；随机U-Net的噪声注入机制为物理不确定性提供了显式建模。
- **高效的潜在空间采样**：将朗之万动力学从像素空间迁移到潜在空间，大幅降低了状态重建的计算复杂度。
- **双轨验证设计**：同时采用模拟数据（定量）和真实卫星数据（定性）验证，兼顾了可控性和操作实用性。

### 8. 不足与局限

- **实验对比不充分**：未与其他同类方法（如三维变分同化、循环神经网络inpainting、扩散模型等）进行横向比较，结论的通用性和相对优势有待进一步验证。
- **真实数据缺少定量评估**：由于缺乏完整真值，仅通过视觉判断，存在主观偏差风险。
- **时间扩展性未验证**：当前仅实现两帧（T=1）序列优化，长时序（T>1）下的收敛性和性能未知。
- **计算成本高**：E步中的朗之万动力学采样需要多步迭代，虽然潜在空间降低了维度，但整体训练开销仍然较大（论文也承认此局限）。
- **物理约束缺失**：未显式保证质量守恒、能量守恒等物理定律，生成的场可能在局部违反动力学约束。
- **理论假设强**：假定状态演化为平稳、遍历、马尔可夫过程，这可能不适用于非平稳或高非线性海洋过程。
- **参数与超参数未充分探索**：未分析不同掩码率、噪声强度、EM迭代次数对结果的影响，也未披露模型参数量。

（完）
