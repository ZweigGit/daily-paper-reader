Title: StocBench: A Benchmark for Generative Modeling of Stochastic Dynamics

URL Source: https://arxiv.org/pdf/2608.22309v1

Published Time: Tue, 25 Aug 2026 01:19:21 GMT

Number of Pages: 19

Markdown Content:
# StocBench: A Benchmark for Generative Modeling of Stochastic Dynamics 

Sebastian Pfister 1 Benjamin Holzschuh 1 Nils Thuerey 1

## Abstract 

We benchmark transport-based generative mod-els as well as distillation-based few-step methods for the probabilistic forecasting of stochastic fluid flows, with a particular focus on performance un-der limited inference budgets. All methods are evaluated on a two-dimensional Kolmogorov flow with stochastic forcing. We measure one-step dis-tributional accuracy against large simulated refer-ence ensembles and assess whether the invariant measure is preserved during autoregressive roll-outs via the enstrophy spectrum. On the stochastic task, flow matching achieves the most accurate one-step conditional distribution at high inference budgets, while the second-order exponential inte-grator DPM-2 is strongest at very low NFE. Few-step distillation methods are competitive with the multi-step methods and preserve the enstrophy spectrum particularly well. A deterministic con-trol task, in which the forcing over the prediction interval is observed, separates aleatoric from epis-temic uncertainty. Model performance does not translate between the two settings: the distilled models are competitive on the stochastic task but least accurate on the control task. While stochas-tic diffusion samplers such as DDPM better pre-serve the enstrophy spectrum during rollouts in the stochastic setting, deterministic samplers such as DDIM and DPM-2 show better spectral preser-vation in the deterministic setting. The benchmark is provided as a ready-to-use Python package at 

https://github.com/tum-pbs/stocb ench .

## 1. Introduction 

Deterministic dynamics can appear stochastic when only part of the system state is observed or when the system is highly chaotic. In these cases, we are often interested in 

> 1

School of Computation, Information and Technology, Techni-cal University of Munich, Germany. Correspondence to: Sebastian Pfister <sebastian.pfister@tum.de >.

Preprint. August 25, 2026. 

the conditional distribution of future states rather than a single best prediction. Examples include probabilistic en-semble weather prediction (Lam et al., 2023; Price et al., 2025), stochastic subgrid-scale parameterization in climate models (Berner et al., 2017), and surrogate modeling of turbulent flows with unresolved fine-scale forcing (Beck et al., 2019). Benchmarks for probabilistic surrogates for stochastic PDEs (SPDEs) have recently begun to emerge: SPDEBench (Li et al., 2025) evaluates neural operators across a range of SPDEs, and the concurrent TRIE frame-work (Srikishan et al., 2026) proposes evaluation criteria for SPDE surrogates. Neither work, however, carefully com-pares transport-based generative methods, such as diffusion models, flow matching, or stochastic interpolation, or their distilled single-step variants, nor analyzes how inference budget affects distributional accuracy. S TOC BENCH focuses precisely on this comparison. With this work, our aim is to provide a benchmarking sce-nario from the regime of turbulent flows that is compara-tively easy to use, and importantly, comes with detailed ground-truth statistics and methods for evaluating distribu-tional accuracy of different methods. A central concept for evaluating generative models of stochastic dynamics is the invariant measure . For a dissipa-tive stochastic system such as stochastically forced Navier– Stokes, trajectories converge after an initial transient to a statistically stationary regime described by a probability dis-tribution μ over the state space that is left unchanged by the dynamics. If ωt ∼ μ and the system evolves one step for-ward, then likewise ωt+∆ t ∼ μ. This long-time distribution encodes physically meaningful statistics, such as the aver-age energy content at each spatial scale. A surrogate that correctly learns the single-step conditional P (ωt+∆ t | ωt)

should, when composed autoregressively, produce trajecto-ries whose long-time statistics also match μ. In practice, even small biases in the learned conditional, for example systematic underestimation of variance or smoothing of high-frequency content, accumulate over rollout steps and cause the model to drift away from the true invariant mea-sure. We evaluate this in the form of the enstrophy spectrum, which describes how vorticity energy is distributed across spatial scales at stationarity and is directly computable from ensemble rollouts. 1arXiv:2608.22309v1 [cs.LG] 23 Aug 2026 StocBench: A Benchmark for Generative Modeling of Stochastic Dynamics 

Overall, our benchmark assesses both one-step conditional probabilistic forecasting, i.e., learning the conditional dis-tribution of the next state given the current state, and how well the invariant measure is respected in long-horizon roll-out scenarios, which is probed via the enstrophy spectrum. Specifically, we consider (i) one-step mean and standard de-viation errors, as well as energy distance, and (ii) the mean difference in the average enstrophy spectrum between the predictions and ensemble references in multi-step autore-gressive rollouts. All reference ensembles are obtained from a numerical simulator. In addition to the probabilistic setting with the stochastic forcing, we evaluate a deterministic variant in which the model is conditioned on the exact forcing. The two vari-ants of our benchmark probe fundamentally different model capabilities. In the stochastic variant, the random forcing is unobserved and the uncertainty in the next state is ir-reducible, i.e., no additional information could remove it. The model must capture a genuine conditional distribution whose spread reflects the unknown driving noise, and au-toregressive rollouts must converge to the invariant measure 

μ. In the deterministic variant, the forcing is provided as input and the next state is fully determined by the current vorticity and forcing together. The task therefore reduces to learning a deterministic simulation step, closely resem-bling the regime studied by neural operators (Li et al., 2021; Brandstetter et al., 2022; Holzschuh et al., 2025). A well-calibrated model should collapse its predictive distribution to a point mass in this setting. The two tasks are thus comple-mentary: the stochastic task tests distributional generation and long-time spectral statistics, while the deterministic task acts as a sanity check that models suppress spurious stochasticity when the physics is fully observed. Addition-ally, comparing the ranking of models in the stochastic and deterministic settings allows us to answer the question: 

Does model performance translate from the stochastic setting to the deterministic one and vice versa? 

For both variants, we evaluate several transport-based mod-els on our benchmark, including DDPM (Ho et al., 2020), DDIM (Song et al., 2021a), DPM-2 (Lu et al., 2022), flow matching (Lipman et al., 2023), and stochastic interpolation (Albergo & Vanden-Eijnden, 2023; Albergo et al., 2025; Chen et al., 2024b). Since transport-based models often perform poorly when the inference budget is very small, we also evaluate distillation methods that are designed to gener-ate samples in only a few steps, or even a single step (Song et al., 2023; Sauer et al., 2024). Throughout, we focus on 

single-step conditional methods that learn P (ωt+∆ t | ωt).Restricting to this setting isolates the quality of the genera-tive step itself from confounding factors such as temporal bundling or extended input histories; we discuss these ex-tensions in the related work. Importantly, StocBench provides a statistically robust eval-uation. The training set comprises 1,500 trajectories with 200 frames each, totaling 300,000 turbulent-flow snapshots. For the one-step evaluation, we estimate ground-truth condi-tional statistics from 5,000 simulated next states for each of 48 conditions, and compare them against model ensembles of 3,000 samples per condition. For the long-term evalua-tion, we unroll 5,000 initial states over 50 autoregressive steps, yielding 250,000 predicted transitions from which we assess whether models preserve the enstrophy spectrum of the invariant measure. To summarize, our contributions are: • We introduce S TOC BENCH , a benchmark for conditional stochastic forecasting in fluid dynamics based on the stochastically forced Kolmogorov flow of Chen et al. (2024b). The benchmark provides datasets and evaluation metrics in a ready-to-use Python package for reproducible model comparison. Special care was taken to provide robust ground truth statistics for evaluation. • We evaluate representative transport-based generative methods on S TOC BENCH and analyze their performance under inference budgets ranging from 10 to 400 network evaluations per sample. We further compare them against distillation-based approaches that generate samples with only one or two network evaluations. • We include a deterministic prediction task in which the model is conditioned on the forcing, yielding dynamics and enstrophy spectra similar to the stochastic variant. This task separates aleatoric from epistemic uncertainty and tests whether model performance translates between the stochastic and deterministic settings. 

## 2. Related Work 

Neural PDE and SPDE benchmarks. Benchmark stud-ies for neural PDE solvers have historically focused on deterministic dynamics and architecture comparisons, using short-horizon prediction error as the primary metric (Li et al., 2021; Lu et al., 2021). PDE-Refiner (Lippe et al., 2023) extends rollout stability of neural PDE solvers by applying iterative denoising refinements, but remains in the determin-istic regime. For stochastic dynamics, SPDEBench (Li et al., 2025) provides a large-scale evaluation comparing architec-tures including FNO (Li et al., 2021), Neural SPDEs (Salvi et al., 2022), and DLR-Net across noise regimes and nu-merical discretizations, but emphasizes architectural dif-ferences over distributional accuracy. For turbulent flows, Kohl et al. (2026) benchmark autoregressive conditional diffusion models on incompressible flows, assessing accu-racy and diversity of generated trajectories. Harder et al. (2025) study probabilistic surrogate modeling for partially observed dynamical systems. Most directly related is the 2StocBench: A Benchmark for Generative Modeling of Stochastic Dynamics 

concurrent TRIE framework (Srikishan et al., 2026), which proposes three evaluation criteria for stochastic PDE sur-rogates: reproduction of invariant measures, calibrated un-certainty estimates via CRPS, and computational efficiency, and compares stochastic interpolants against deterministic neural architectures on stochastic Kolmogorov flow and the Kuramoto–Sivashinsky equation. S TOC BENCH shares the focus on distributional evaluation but distinguishes itself by systematically comparing the full class of transport-based generative methods and distillation approaches under con-trolled inference budgets. 

Transport-based generative models for physical dynam-ics. Score-based diffusion models (Ho et al., 2020; Song et al., 2021b) and flow matching (Lipman et al., 2023; Liu et al., 2023) have seen broad application in physical simu-lation and forecasting. For weather prediction, Lam et al. (2023) demonstrate that deterministic neural models can match numerical weather prediction, while probabilistic variants generate calibrated ensemble forecasts (Price et al., 2025). Stochastic interpolation (Albergo & Vanden-Eijnden, 2023; Chen et al., 2024b; Albergo et al., 2025) provides a unified framework for constructing stochastic transport pro-cesses between arbitrary distributions, including between consecutive simulation snapshots. All these methods share a common structure. They map a simple prior to the tar-get distribution via an ODE or SDE, and converge to the correct distribution as the number of integration steps in-creases. S TOC BENCH provides a controlled evaluation of their relative strengths under limited inference budgets. 

Few-step and single-step inference. Consistency mod-els (Song et al., 2023; Song & Dhariwal, 2024) and adver-sarial diffusion distillation (Sauer et al., 2024) compress multi-step transport into one or a few network evaluations by distilling a multi-step teacher. While distillation methods trade theoretical convergence guarantees for inference speed, this trade-off is important for ensemble-based probabilis-tic forecasting, which requires many independent samples. Chen et al. (2025) show theoretically that the benefit of ad-ditional consistency sampling steps depends on the rate at which training error decreases, explaining the empirically observed saturation of quality gains beyond two steps. We include both distillation approaches in our benchmark to assess whether they can match or exceed transport-based methods under the limited inference budgets where the latter are most disadvantaged. 

Multi-frame prediction and extended temporal context. 

The ti → ti+1 formulation we study is the most fundamen-tal and tightly controlled setting for benchmarking genera-tive models of stochastic dynamics, but two natural exten-sions are often employed in practice. First, models can be trained to predict multiple future frames {ti+1 , . . . , t i+n}        

> t=0 t=1 t=2 t=3
> Initial state Next state Mean Std
> −2.50.02.5
> vort.
> 0.40.60.8
> std
> Figure 1. Dataset preview for stochastic Kolmogorov flow. First row: consecutive vorticity snapshots. Second row: next-state ensemble statistics for a fixed initial condition, with the mean and standard deviation estimated from 3000 samples.

jointly in a single forward pass, which is known as temporal bundling (Brandstetter et al., 2022). Temporal bundling addresses exposure bias , i.e., a model trained on single-step predictions is applied at inference time to its own outputs rather than ground-truth inputs, and small prediction errors compound over long rollouts. Predicting n steps jointly aligns the training and inference distributions more closely, reducing this compounding effect. Second, models can con-dition on an extended input history {ti−k, . . . , t i} rather than only the current state, providing additional temporal context that can reduce prediction uncertainty and improve long-term physical consistency (Shysheya et al., 2024). Dif-fusion Forcing (Chen et al., 2024a) unifies these ideas in a probabilistic framework. By applying independent per-token noise levels across an entire multi-step sequence, it enables joint generation over full trajectories with diffusion-style iterative refinement and flexible conditioning on any subset of past frames. S TOC BENCH deliberately restricts to the ti → ti+1 setting to provide the most controlled com-parison of generative modeling and distillation strategies, isolating the quality of individual generative steps from the confounding effects of temporal bundling or extended his-tory conditioning. Extending the benchmark to cover these richer formulations is a natural direction for future work. 

## 3. Benchmark 

Our benchmark evaluates models on two variants of Kol-mogorov flow. Stochastic Kolmogorov flow assesses whether generative models can reproduce the stochasticity induced by unobserved random forcing, while deterministic Kolmogorov flow tests whether the models make determin-istic predictions when the forcing is observed. Dataset gen-eration details for both variants are provided in Appendix A. 

3.1. Stochastic Kolmogorov Flow 

Following Chen et al. (2024b), we simulate the incompress-ible Navier–Stokes equations on the two-dimensional torus 3StocBench: A Benchmark for Generative Modeling of Stochastic Dynamics 

T2 = [0 , 2π]2 with stochastic forcing. For x = ( x, y ) ∈ T2

and t ∈ [0 , T ], the vorticity field ω(x, t ) evolves according to the vorticity–streamfunction formulation 

dω = [ −(v · ∇ )ω + ν∆ω − αω ] dt + ϵdη, (1) where v is the velocity field, ν > 0 is the kinematic viscos-ity, α > 0 is a linear drag (Rayleigh friction) coefficient that dissipates large-scale energy, and ϵ > 0 scales the stochastic forcing. The forcing increment dη is a linear combination of fixed basis functions scaled by the differentials of inde-pendent Wiener processes Wk and Vk:

dη (x, t ) = X

> k∈K

sin( k · x) dW k + cos( k · x) dV k, (2) where K = {(6 , 0) , (7 , 0) , (5 , 5) , (8 , 8) } is the set of forced wavenumbers. The forecasting task is to learn the conditional next-state distribution P (ωt+∆ t|ωt), where uncertainty arises from the unobserved forcing realization over the interval [t, t + ∆ t].Figure 1 shows consecutive vorticity snapshots together with the next-state ensemble statistics for a fixed initial condition. 

3.2. Deterministic Kolmogorov Flow 

In the second variant, we make the prediction step deter-ministic by conditioning on the forcing. Rather than using Wiener increments, we sample a forcing ηt for each snapshot interval [t, t + ∆ t] and hold it fixed throughout the interval. Given the current vorticity ωt and the applied forcing ηt, the next vorticity ωt+∆ t is deterministic up to numerical solver accuracy. Although the stochastic variant is driven by a Wiener pro-cess, whereas the deterministic variant keeps the forcing fixed over each snapshot interval, their average enstrophy spectra are very similar, as shown in Figure 2. 

3.3. Metrics 

In the stochastic setting, the current state ωt does not uniquely determine the next state, but instead defines a conditional distribution P (ωt+∆ t | ωt). To assess how well each baseline model distribution Qθ (ωt+∆ t | ωt) approxi-mates the true next-snapshot distribution P , we use three one-step metrics; a fourth metric evaluates whether the in-variant measure is preserved during autoregressive rollouts: 1. Conditional Mean Error (ϵμ): Quantifies the error in the expected next state. Let μP and μQ denote the pointwise ensemble means of the true distribution P

and the predicted distribution Qθ, respectively. The relative L2 error is 

ϵμ = ∥μP − μQ∥2

∥μP ∥2

.   

> 10 010 1
> Wavenumber k
> 10 −2
> 10 −1
> 10 0
> 10 1
> 10 2
> 10 3
> Enstrophy
> Stochastic KF Deterministic KF
> Figure 2. Both Kolmogorov-flow variants have very similar aver-age radial enstrophy spectra.

2. Conditional Standard Deviation Error (ϵσ ): Quanti-fies the error in the predicted stochastic spread. Let σP

and σQ denote the corresponding pointwise conditional standard deviations. The relative L2 error is 

ϵσ = ∥σP − σQ∥2

∥σP ∥2

.

3. Energy Distance (ϵD): Quantifies the mismatch be-tween the predicted and true next-snapshot conditional distributions beyond their first two moments. For a grid point (i, j ), the energy distance between the model dis-tribution and the true distribution is defined as 

Di,j = 2 E|x − y| − E|x − x′| − E|y − y′|,

where x, x ′ are independent samples from Qθ at grid point (i, j ), and y, y ′ are independent samples from P

at the same grid point. We define 

ϵD = 1

NxNyNxX

> i=1
> Ny

X

> j=1

Di,j 

as the spatial average of the pointwise energy distance, estimated from ensembles of generated and ground-truth samples. This metric is closely related to the continuous ranked probability score (CRPS; see, e.g., Gneiting & Raftery, 2007) and is used when the full reference distribution is available rather than a single observed outcome. 4. Enstrophy Spectrum Error (ϵZ ): Quantifies how well the invariant measure μ is preserved during autoregres-sive rollouts. Starting from an ensemble of states drawn from μ, we compare the ensemble-averaged radial en-strophy spectra of the model, ˆZt(k), and the solver, 

Zt(k), at each rollout step t and radial wavenumber k.4StocBench: A Benchmark for Generative Modeling of Stochastic Dynamics 

The mean absolute error over Troll rollout steps and K

radial bins is 

ϵZ = 1

Troll K

> Troll

X

> t=1
> K

X

> k=1

Zt(k) − ˆZt(k) .

## 4. Baselines 

Most multi-step generative models can be formulated as continuous-time transport processes governed by stochas-tic differential equations (SDEs) or ordinary differential equations (ODEs) (Song et al., 2021b). This unified frame-work makes the baselines directly comparable. By varying the number of solver steps, we can analyze the trade-off between distributional fidelity and inference cost. We quan-tify inference cost by the number of function evaluations (NFEs), i.e., the number of network forward passes required to generate a sample. Throughout this section, t denotes physical time and τ the transport time of the generative process. Diffusion models transport noise at τ = 1 to data at τ = 0 , whereas flow matching and stochastic interpolation run in the opposite direction, from noise at τ = 0 to data at τ = 1 . All baselines learn P (ωt+∆ t | ωt) and receive the conditioning state ωt

as an additional network input, which we suppress in the notation. 

4.1. Diffusion Models 

Diffusion models learn to reverse a forward process that gradually corrupts data with Gaussian noise (Ho et al., 2020). In the limit of infinitely many diffusion steps, the forward process can be described by an SDE of the form 

dx = f (x, τ ) dτ + g(τ ) dw, (3) where τ ∈ [0 , 1] is the diffusion time, f (x, τ ) is the drift coefficient, g(τ ) is the diffusion coefficient, and w is a Wiener process (Song et al., 2021b). We adopt the variance-preserving (VP) noise schedule β(τ ) from DDPM (Ho et al., 2020), corresponding to f (x, τ ) = − 12 β(τ )x and g(τ ) = 

pβ(τ ). The reverse process is described by the SDE 

dx = f (x, τ ) − g(τ )2∇x log pτ (x) dτ + g(τ ) d ¯w, (4) where pτ denotes the marginal density of the forward pro-cess at diffusion time τ and ¯w is a Wiener process running backward in time. The Fokker–Planck equation further im-plies that every such diffusion process admits a deterministic probability flow ODE with the same marginals pτ (x) (Song et al., 2021b), which enables deterministic sampling. This ODE is given by 

dx = f (x, τ ) − 12 g(τ )2∇x log pτ (x) dτ. (5) In both the reverse SDE (4) and the probability flow ODE (5), the score function ∇x log pτ (x) is the only unknown term. In practice, it is expressed in terms of the noise pre-dictor ϵθ (x, τ ) using the approximation ∇x log pτ (xτ ) ≈−ϵθ (xτ , τ )/σ τ , where στ is the standard deviation of the forward perturbation kernel pτ (xτ | x0) around a clean sample x0. The network ϵθ is trained to predict the noise added by the forward process (Ho et al., 2020). Sampling draws from the standard Gaussian prior and in-tegrates either the SDE (4) or the ODE (5) from τ = 1 to 

τ = 0 . Our benchmark compares three samplers that share a single trained ϵθ-prediction model and differ only in the numerical integration scheme: • DDIM (Song et al., 2021a) is a first-order exponential integrator for the ODE (5): the linear drift is integrated exactly, and ϵθ is held constant at its value at the start of each step. Each step requires one NFE. • DPM-2 is the second-order exponential integrator DPM-Solver-2 (Lu et al., 2022): the linear drift is again exact, and the ϵθ integral is approximated by a midpoint rule. Each step therefore requires two NFEs. • DDPM (Ho et al., 2020) performs ancestral sampling: each update draws from the Gaussian posterior of the discretized forward process, with the clean state re-placed by its ϵθ -based estimate. This corresponds to a first-order stochastic discretization of the reverse SDE (4) (Song et al., 2021b). Each step requires one NFE. 

4.2. Flow Matching 

Flow matching (Lipman et al., 2023) learns a time-dependent vector field uτ (x) whose flow transports a Gaus-sian prior to the target distribution. Because this marginal vector field is intractable, Lipman et al. (2023) introduce conditional flow matching: a neural network vθ is regressed onto the conditional vector field uτ (x | x1), which is avail-able in closed form for a fixed data sample x1. Lipman et al. (2023) show that this objective has the same minimizer as regression onto the intractable marginal field. We use the conditional optimal transport (OT) path, which interpolates linearly between noise x0 ∼ N (0 , I ) and a data sample x1, with conditional vector field 

uτ (x | x1) = x1 − (1 − σmin )x

1 − (1 − σmin )τ , (6) where σmin > 0 is a small constant controlling the resid-ual noise at τ = 1 (see Table 2). Along this path, each conditional trajectory is a straight line traversed at constant speed. In practice, this also results in straighter marginal trajectories than those of the probability flow ODE (5), so fewer steps suffice for accurate integration. To generate samples, we draw noise from the prior and numerically integrate the learned ODE defined by vθ from 5StocBench: A Benchmark for Generative Modeling of Stochastic Dynamics 

τ = 0 to τ = 1 . For our FM baseline, we use the standard Euler method, at a cost of one NFE per step. 

4.3. Stochastic Interpolation 

Stochastic interpolation defines a time-dependent process 

Iτ that smoothly transitions between a base observation x0

and a target x1:

Iτ = ατ x0 + βτ x1 + στ Wτ , τ ∈ [0 , 1] . (7) Here, W = ( Wτ )τ ∈[0 ,1] is a Wiener process, and ατ , βτ ,and στ are differentiable scalar schedules satisfying bound-ary conditions that ensure I0 = x0 and I1 = x1; specifically, 

α0 = β1 = 1 and α1 = β0 = σ1 = 0 . In contrast to the pre-vious methods, both endpoints are physical states: x0 = ωt

is the conditioning state and x1 = ωt+∆ t the next snap-shot, so the process bridges consecutive states instead of transporting from a Gaussian prior. Chen et al. (2024b) show that, for x1 ∼ q(x1 | x0), there exists an SDE whose marginal distributions match those of the interpolation process Iτ . This SDE is defined by 

dX τ = bτ (Xτ , x 0) dτ + στ dW τ , (8) where the drift coefficient bτ (x, x 0) can be learned by mini-mizing the regression objective 

Eτ,x 0,x 1,W 

h

∥bτ (Iτ , x 0) − Rτ ∥2i

, (9) with target vector field Rτ = ˙ ατ x0 + ˙βτ x1 + ˙ στ Wτ .For our SI baseline, we follow Chen et al. (2024b) and use their interpolant schedules (Table 2). We then numeri-cally integrate the corresponding SDE (8) using the Euler– Maruyama method, at a cost of one NFE per step. 

4.4. Consistency Distillation 

Consistency models (Song et al., 2023) enforce a self-consistency constraint across different noise levels of the same denoising trajectory, enabling sampling in far fewer steps. We formalize this constraint with a consistency func-tion f : Rd × [0 , T ] → Rd that maps a noisy state xτ at noise time τ into data space. For two states xτ and xτ ′ lying on the same denoising trajectory, consistency requires 

f (xτ , τ ) = f (xτ ′ , τ ′). (10) Together with the boundary condition f (x0, 0) = x0, this ensures that every point along a given trajectory maps to the exact same clean sample x0.Consistency distillation trains fθ by enforcing this constraint along the probability flow ODE of a pretrained teacher diffu-sion model rather than learning a consistency function from scratch. This teacher-guided objective is generally more stable and yields higher sample quality. Sampling draws from the prior and evaluates fθ at the maximum noise level. For multi-step generation, this prediction is refined by alter-nately adding noise and re-evaluating at progressively lower noise levels. In our benchmark, we evaluate consistency distillation using single- and multi-step sampling. • CD-1: Evaluates the consistency function once at the maximum noise level. • CD-2: Uses one additional evaluation at the noise level that is most effective for the specific forecasting task (see Appendix C). 

4.5. Adversarial Diffusion Distillation 

Adversarial Diffusion Distillation (ADD) (Sauer et al., 2024) extends the standard generative adversarial network (GAN) setup (Goodfellow et al., 2020), consisting of a generator and a discriminator, by introducing a frozen, pretrained teacher model. The discriminator learns to distinguish be-tween real and generated samples, assigning high scores to real data and low scores to generated data. The generator optimizes a dual objective: a distillation loss that minimizes the discrepancy between student and teacher samples, and an adversarial loss that maximizes the discriminator score. Importantly, because of adversarial guidance, the generator is not strictly limited by the sampling quality of the teacher (Sauer et al., 2024). While ADD originally uses diffusion models, our ADD-FM baseline adapts the framework to flow matching using a one-step generator and a 10-step teacher. Sampling there-fore requires a single NFE. Training details are given in Appendix B.5. 

## 5. Experiments 

We organize the experiments into two parts. First, we evalu-ate all baselines on stochastic Kolmogorov flow using the metrics defined in Section 3.3. This setting tests whether the models can learn dynamics with inherent stochasticity. Second, we evaluate the same baselines on the determin-istic variant to assess whether they produce accurate and deterministic predictions when the conditioning information fully determines the next state. All models are trained on the full training set and evaluated on 48 initial conditions with an ensemble size of 3,000; further details are given in Appendix A. During evaluation, we use the exponential moving average (EMA) version of each model with a decay rate of 0.999 . Implementation details for each model are provided in Appendix B. 6StocBench: A Benchmark for Generative Modeling of Stochastic Dynamics                                 

> 050 100 150 200 250 300 350 400
> NFE
> 0.00 0.02 0.04 0.06 0.08 0.10
> Relative L2 error (mean) (a)
> 050 100 150 200 250 300 350 400
> NFE
> 0.00 0.02 0.04 0.06 0.08 0.10
> Relative L2 error (std) (b)
> 050 100 150 200 250 300 350 400
> NFE
> 0.00 0.02 0.04 0.06 0.08 0.10
> Energy distance (c)
> 050 100 150 200 250 300 350 400
> NFE
> 0510 15 20 25 30 35 40
> Enstrophy spectrum error (d)
> SI DPM-2 DDIM DDPM FM CD-1 CD-2 ADD-FM

Figure 3. Metrics for stochastic Kolmogorov flow that measure one-step distributional fit (a–c) as well as preservation of the invariant measure μ during autoregressive rollout, assessed in the enstrophy spectrum (d). All metrics are defined in Section 3.3. Lower values indicate closer agreement with the reference distribution. Markers denote the inference budget while dotted and dashed lines indicate models using 1 and 2 NFEs, respectively. 

5.1. Stochastic Kolmogorov Flow 

The stochastic task evaluates whether the methods can re-produce the conditional next-state distribution induced by unobserved forcing. We assess one-step distributional fit with mean and standard deviation errors, as well as energy distance, which captures differences beyond the first two moments. We also evaluate whether the models preserve the invariant measure μ. To do so, we initialize autoregressive rollouts from an ensemble of states sampled from μ and compare the enstrophy spectrum after rollout with that of μ.In terms of mean error, flow matching (FM) performs best across all inference budgets (Figure 3a). This behavior is consistent with flow matching learning comparatively straight transport trajectories, whereas diffusion-based meth-ods such as DDPM, DDIM, and DPM-2 follow more curved probability-flow paths. At higher inference budgets, stochastic interpolation (SI) begins to outperform the other diffusion methods, yet still leaves a gap to FM. Since SI interpolates between consecutive simulation snapshots, its endpoints are typically close in state space, resulting in less curved stochastic bridges than trajectories initialized from a generic Gaussian distribution. Among the distilled mod-els, adversarial diffusion distillation (ADD-FM) achieves a substantially lower mean error than consistency distillation, likely due to its strong flow matching teacher. Adding a second NFE at an optimized noise level (CD-2) slightly im-proves the mean error over CD-1 (Appendix C). Figure 12 shows the corresponding conditional mean and standard deviation error fields for three example conditions. For the standard deviation error in Figure 3b, DPM-2 per-forms particularly well at low inference budgets. Its error exhibits a sharp minimum at only 20 NFEs, where DPM-2 outperforms all other multi-step baselines. This minimum likely stems from coarse discretization effects that com-pensate for the model’s systematic underestimation of the standard deviation (Figure 5, left). For higher inference 7StocBench: A Benchmark for Generative Modeling of Stochastic Dynamics       

> 010 20 30 40 50
> Rollout step
> 0246810 12 14 16
> Enstrophy spectrum error
> SI DPM-2 DDIM DDPM FM CD-1 CD-2 ADD-FM
> 10 010 1
> Wavenumber k
> 0.60.81.01.21.41.61.82.0
> Enstrophy ratio
> SI DPM-2 DDIM DDPM FM CD-1 CD-2 ADD-FM

Figure 4. Evaluation of model invariant measures for stochastic Kolmogorov flow, obtained from autoregressive rollouts and assessed by the enstrophy spectrum. Left : enstrophy spectrum error at each step of a 50-step rollout. Right : ratio of predicted to reference enstrophy after 50 rollout steps; values above the black line indicate overestimation, while values below indicate underestimation. In both subplots, dotted, dashed, and solid lines denote models using 1, 2, and 400 NFEs, respectively. 

budgets, FM achieves the lowest standard deviation error, with SI slowly approaching. Notably, ADD-FM matches the performance of FM with a single inference step. Figure 5 (left) shows one-step predictive variability , cal-culated as the spatial L2 norm of the pointwise ensemble standard deviation. Most methods underestimate variability at low inference budgets and approach the ground truth as the number of NFEs increases. The only exception is DPM-2, which overestimates the conditional standard deviation at low budgets. The invariant-measure evaluations (Figures 3d and 4) sug-gest that deterministic denoising increases spectral damping. Deterministic sampling acts as a spectral filter, whereas stochastic sampling preserves small-scale energy through the injected noise. This behavior is most evident for the diffusion models, which share the same ϵ-prediction model. DDIM and DPM-2 converge to invariant measures with lower enstrophy than the ground-truth reference, whereas DDPM retains most of the enstrophy. FM is the excep-tion among the multi-step methods, achieving the lowest enstrophy spectrum error at high inference budgets, with an enstrophy error that remains nearly constant throughout the 50-step rollout (Figure 4, left). The corresponding spectra after 50 rollout steps are shown in Figure 10. 

5.2. Deterministic Kolmogorov Flow 

To assess whether the predictive variability observed on the stochastic task reflects the intrinsic stochasticity of the dynamics, we evaluate all models on a deterministic con-trol task. Predictive variability has two possible sources: 

aleatoric uncertainty stems from the stochastic forcing and is irreducible, whereas epistemic uncertainty stems from imperfect learning of the dynamics and would vanish for a perfect model. The control task eliminates the aleatoric source by conditioning the models on the forcing over the next snapshot interval, so that the next state is fully de-termined. Any residual variability is therefore epistemic. Figure 5 reports the predictive variability for both tasks. Overall, single- and few-step distillation methods exhibit higher residual variability than multi-step methods (Figure 5, right), indicating greater epistemic uncertainty in predict-ing the next step with only one or a few inference steps. Among the single-step methods, CD-1 shows substantially higher residual variability than ADD-FM. This contrasts with the stochastic setting, where CD-1 accurately recovers the conditional distribution using a single inference step. As shown in Appendix C, this discrepancy arises from the sam-pling schedule: for stochastic dynamics, a single evaluation at the highest noise level is sufficient, whereas determinis-tic dynamics require additional lower-noise evaluations to eliminate residual variability. The mean error shows an even larger gap between distilla-tion and multi-step methods (Figure 6, left). The multi-step methods form a tight cluster with low errors, while the distilled models have significantly higher errors. Figure 6 (right) evaluates long-term accuracy by autoregressively un-rolling an ensemble of randomly sampled initial conditions for 50 steps and reporting the ensemble-averaged RMSE relative to the deterministic solver trajectory. Single- and few-step distillation methods deviate faster from the solver trajectory, while all models maintain stable rollouts over the full rollout horizon. Figure 7 shows that preserving spatial structures during long autoregressive rollouts is easier in the deterministic setting than in the stochastic setting. Except for CD-1, all ensemble-averaged enstrophy spectra remain close to the ref-8StocBench: A Benchmark for Generative Modeling of Stochastic Dynamics                 

> 050 100 150 200 250 300 350 400
> NFE
> 25 .526 .026 .527 .027 .528 .028 .529 .029 .530 .0
> L2 error (std) Stochastic Kolmogorov flow
> SI DPM-2 DDIM DDPM FM CD-1 CD-2 ADD-FM Ground truth
> 050 100 150 200 250 300 350 400
> NFE
> 10 0
> 10 1
> L2 error (std) Deterministic Kolmogorov flow
> SI DPM-2 DDIM DDPM FM CD-1 CD-2 ADD-FM

Figure 5. Variability of one-step predictions for the stochastic (left) and deterministic (right) Kolmogorov flow as a function of NFE. Markers denote the inference budget while dotted and dashed lines indicate models using 1 and 2 NFEs, respectively.                        

> 050 100 150 200 250 300 350 400
> NFE
> 10 −1
> 3×10 −2
> 4×10 −2
> 6×10 −2
> Relative L2 error (mean) Deterministic Kolmogorov flow
> SI DPM-2 DDIM DDPM FM CD-1 CD-2 ADD-FM
> 510 15 20 25 30 35 40 45 50

Rollout step 

> 0.00.20.40.60.81.01.2
> RMSE
> SI DPM-2 DDIM DDPM FM CD-1 CD-2 ADD-FM

Figure 6. Accuracy on deterministic Kolmogorov flow relative to the numerical solver. Left : For each inference budget, we report the error between the ensemble mean of the one-step predictions and the deterministic ground-truth next snapshot generated by the numerical solver. Markers denote the inference budget, with dotted and dashed lines corresponding to 1 and 2 NFEs, respectively. Right : RMSE at each rollout step, averaged over autoregressive trajectories initialized from randomly sampled states. Markers denote the inference budget, while dotted, dashed, and solid lines correspond to evaluations using 1, 2, and 400 NFEs, respectively. 

erence spectrum. Unlike in the stochastic setting, distillation methods do not consistently outperform multi-step methods. In particular, CD-1 exhibits excessive enstrophy, especially at high-frequency modes (Figure 7, right). The absolute spectra after 50 rollout steps are shown in Figure 11. 

## 6. Discussion 

The results paint an interesting picture of the pros and cons of different generative samplers as numerical instruments for stochastic physics: • Flow matching (FM) is the most accurate method at high inference budgets on the stochastic task: it achieves the best one-step conditional distribution and, among the multi-step samplers, best preserves the en-strophy spectrum. • DPM-2 has advantages in the low-budget regime: its exponential integrator yields accurate conditional dis-tributions with only a few NFEs, especially for the standard deviation (Figure 3b). • The three diffusion samplers DDIM, DPM-2 and DDPM share a single trained model and differ only in the sampling procedure, yet on the stochastic task their enstrophy spectrum errors over autoregressive rollouts differ by a factor of five: DDIM and DPM-2 converge to a damped invariant measure, whereas DDPM preserves it (Figure 4). On the deterministic task this ranking reverses (Figure 7). The stochastic updates of DDPM reintroduce small-scale enstrophy, which is beneficial in the stochastic setting but results in excessive enstrophy in the deterministic one. The one-step metrics of the diffusion samplers are very sim-ilar, so one-step accuracy alone does not imply spectral 9StocBench: A Benchmark for Generative Modeling of Stochastic Dynamics       

> 010 20 30 40 50
> Rollout step
> 0510 15 20 25
> Enstrophy spectrum error
> SI DPM-2 DDIM DDPM FM CD-1 CD-2 ADD-FM
> 10 010 1
> Wavenumber k
> 0.60.81.01.21.41.61.82.0
> Enstrophy ratio
> SI DPM-2 DDIM DDPM FM CD-1 CD-2 ADD-FM

Figure 7. Evaluation of model invariant measures for deterministic Kolmogorov flow, obtained from autoregressive rollouts and assessed by the enstrophy spectrum. Left : enstrophy spectrum error at each rollout step. Right : ratio of predicted to reference enstrophy after 50 rollout steps; values above the black line indicate overestimation, while values below indicate underestimation. In both subplots, dotted, dashed, and solid lines denote models using 1, 2, and 400 NFEs, respectively. 

accuracy over long rollouts. • Distillation methods can be highly competitive, with ADD-FM in particular showing strong preservation of the invariant measure at a single NFE (Figure 4). However, their training often does not converge mono-tonically, and part of their predictive variability is epis-temic, i.e., it stems from imperfect learning rather than the stochastic forcing (Figure 5, right). • Stochastic interpolation (SI) is intuitive, as it bridges nearby simulation states rather than transporting from pure noise. Nevertheless, it needs a relatively large inference budget to become competitive. Our benchmark shows that no single method dominates all regimes. Flow matching is the most accurate method on the stochastic task, but only at high inference budgets. Distilla-tion methods are competitive using only one or two NFEs on the stochastic task, yet fall behind all multi-step samplers in the deterministic setting. DPM-2 is the strongest multi-step method at low budgets and performs consistently in both settings, though its accuracy plateaus early and does not improve at higher budgets. 

## 7. Conclusion 

We have introduced S TOC BENCH , a benchmark for evalu-ating generative modeling of stochastic dynamics. Which method is preferable depends on the inference budget: flow matching is the most accurate at high budgets, while the distilled models are competitive at a single NFE on the stochastic task. At low budgets, DPM-2 is the strongest multi-step method and, unlike the distilled models, does not suffer from unstable training. We find that performance does not translate between the stochastic setting and that of the deterministic control task. The distilled models are competitive on the stochastic task but least accurate on the control task, suggesting that part of their predictive variability is epistemic, i.e., caused by imperfect learning rather than the inherent stochasticity of the dynamics. Among the diffusion samplers, the ranking is reversed between the two settings: the stochastic sampler DDPM best preserves the rollout enstrophy spectrum on the stochastic task, whereas the deterministic samplers DDIM and DPM-2 preserve it better on the deterministic one. While we focused on making S TOC BENCH as accessible as possible to guide the development of future generative models and distillation algorithms for stochastic dynamics, future work could provide benchmark scenarios for higher dimensions, resolutions, and other stochastic systems. 10 StocBench: A Benchmark for Generative Modeling of Stochastic Dynamics 

## References 

Albergo, M., Boffi, N. M., and Vanden-Eijnden, E. Stochas-tic interpolants: A unifying framework for flows and diffusions. Journal of Machine Learning Research , 26 (209):1–80, 2025. URL http://jmlr.org/paper s/v26/23-1605.html .Albergo, M. S. and Vanden-Eijnden, E. Building normaliz-ing flows with stochastic interpolants. In The Eleventh International Conference on Learning Representations, ICLR 2023, Kigali, Rwanda, May 1-5, 2023 , 2023. URL 

https://openreview.net/forum?id=li7q eBbCR1t .Beck, A., Flad, D., and Munz, C.-D. Deep neural net-works for data-driven LES closure models. Journal of Computational Physics , 398:108910, 2019. doi: 10.1016/j.jcp.2019.108910. URL http://dx.d oi.org/10.1016/j.jcp.2019.108910 .Berner, J., Achatz, U., Batt´ e, L., Bengtsson, L., de la C´ amara, A., Christensen, H. M., Colangeli, M., Coleman, D. R. B., Crommelin, D., Dolaptchiev, S. I., Franzke, C. L. E., Friederichs, P., Imkeller, P., J¨ arvinen, H., Ju-ricke, S., Kitsios, V., Lott, F., Lucarini, V., Mahajan, S., Palmer, T. N., Penland, C., Sakradzija, M., von Storch, J.-S., Weisheimer, A., Weniger, M., Williams, P. D., and Yano, J.-I. Stochastic parameterization: Toward a new view of weather and climate models. Bulletin of the Amer-ican Meteorological Society , 98(3):565–588, 2017. doi: 10.1175/bams-d-15-00268.1. URL http://dx.doi .org/10.1175/BAMS-D-15-00268.1 .Berthelot, D., Schumm, T., and Metz, L. BEGAN: bound-ary equilibrium generative adversarial networks. CoRR ,abs/1703.10717, 2017. URL http://arxiv.org/ abs/1703.10717 .Brandstetter, J., Worrall, D. E., and Welling, M. Message passing neural PDE solvers. In The Tenth International Conference on Learning Representations, ICLR 2022, Virtual Event, April 25-29, 2022 , 2022. URL https: //openreview.net/forum?id=vSix3HPYKSU .Chen, B., Monso, D. M., Du, Y., Simchowitz, M., Tedrake, R., and Sitzmann, V. Diffusion forcing: Next-token pre-diction meets full-sequence diffusion. In Advances in Neural Information Processing Systems 37: Annual Con-ference on Neural Information Processing Systems 2024, NeurIPS 2024, Vancouver, BC, Canada, December 10 -15, 2024 , 2024a. URL http://papers.nips.cc /paper_files/paper/2024/hash/2aee1c4 159e48407d68fe16ae8e6e49e-Abstract-C onference.html .Chen, Y., Goldstein, M., Hua, M., Albergo, M. S., Boffi, N. M., and Vanden-Eijnden, E. Probabilistic forecasting with stochastic interpolants and f¨ ollmer processes. In 

Forty-first International Conference on Machine Learn-ing, ICML 2024, Vienna, Austria, July 21-27, 2024 , vol-ume 235, pp. 6728–6756, 2024b. URL https://pr oceedings.mlr.press/v235/chen24n.html .Chen, Y., Zhang, Y., Oertell, O., and Sun, W. Convergence of consistency model with multistep sampling under gen-eral data assumptions. In Forty-second International Conference on Machine Learning, ICML 2025, Vancou-ver, BC, Canada, July 13-19, 2025 , volume 267, 2025. URL https://proceedings.mlr.press/v2 67/chen25ck.html .Gneiting, T. and Raftery, A. E. Strictly proper scoring rules, prediction, and estimation. Journal of the American Statistical Association , 102(477):359–378, 2007. doi: 10.1198/016214506000001437. URL http://dx.d oi.org/10.1198/016214506000001437 .Goodfellow, I., Pouget-Abadie, J., Mirza, M., Xu, B., Warde-Farley, D., Ozair, S., Courville, A., and Bengio, Y. Generative adversarial networks. Communications of the ACM , 63(11):139–144, 2020. doi: 10.1145/3422622. URL http://dx.doi.org/10.1145/3422622 .Harder, H., Vishwasrao, A., Guastoni, L., Vinuesa, R., and Peitz, S. Efficient probabilistic surrogate mod-eling techniques for partially-observed large-scale dy-namical systems. CoRR , abs/2511.04641, 2025. doi: 10.48550/ARXIV.2511.04641. URL https://doi. org/10.48550/arXiv.2511.04641 .Ho, J., Jain, A., and Abbeel, P. Denoising diffusion prob-abilistic models. In Advances in Neural Information Processing Systems 33: Annual Conference on Neural Information Processing Systems 2020, NeurIPS 2020, December 6-12, 2020, virtual , 2020. URL https: //proceedings.neurips.cc/paper/2020/ hash/4c5bcfec8584af0d967f1ab10179ca4 b-Abstract.html .Holzschuh, B. J., Liu, Q., Kohl, G., and Thuerey, N. Pde-transformer: Efficient and versatile transformers for physics simulations. In Forty-second International Con-ference on Machine Learning, ICML 2025, Vancouver, BC, Canada, July 13-19, 2025 , volume 267, 2025. URL 

https://proceedings.mlr.press/v267/h olzschuh25a.html .Karras, T., Aittala, M., Aila, T., and Laine, S. Elucidating the design space of diffusion-based generative models. In 

Advances in Neural Information Processing Systems 35: Annual Conference on Neural Information Processing Systems 2022, NeurIPS 2022, New Orleans, LA, USA, 

11 StocBench: A Benchmark for Generative Modeling of Stochastic Dynamics 

November 28 - December 9, 2022 , 2022. URL http: //papers.nips.cc/paper_files/paper/2 022/hash/a98846e9d9cc01cfb87eb694d94 6ce6b-Abstract-Conference.html .Kohl, G., Chen, L., and Thuerey, N. Benchmarking autore-gressive conditional diffusion models for turbulent flow simulation. Neural Networks , 199:108641, 2026. doi: 10.1016/J.NEUNET.2026.108641. URL https://do i.org/10.1016/j.neunet.2026.108641 .Lam, R., Sanchez-Gonzalez, A., Willson, M., Wirnsberger, P., Fortunato, M., Alet, F., Ravuri, S., Ewalds, T., Eaton-Rosen, Z., Hu, W., Merose, A., Hoyer, S., Holland, G., Vinyals, O., Stott, J., Pritzel, A., Mohamed, S., and Battaglia, P. Learning skillful medium-range global weather forecasting. Science , 382(6677):1416–1421, 2023. doi: 10.1126/science.adi2336. URL http: //dx.doi.org/10.1126/science.adi2336 .Li, Z., Kovachki, N. B., Azizzadenesheli, K., Liu, B., Bhat-tacharya, K., Stuart, A. M., and Anandkumar, A. Fourier neural operator for parametric partial differential equa-tions. In 9th International Conference on Learning Rep-resentations, ICLR 2021, Virtual Event, Austria, May 3-7, 2021 , 2021. URL https://openreview.net/f orum?id=c8P9NQVtmnO .Li, Z., Zhu, Y., Ni, H., Li, S., Chen, B., and Meng, Q. Spdebench: An extensive benchmark for learning regular and singular stochastic pdes. CoRR , abs/2505.18511, 2025. doi: 10.48550/ARXIV.2505.18511. URL https: //doi.org/10.48550/arXiv.2505.18511 .Lipman, Y., Chen, R. T. Q., Ben-Hamu, H., Nickel, M., and Le, M. Flow matching for generative modeling. In The Eleventh International Conference on Learning Repre-sentations, ICLR 2023, Kigali, Rwanda, May 1-5, 2023 ,2023. URL https://openreview.net/forum ?id=PqvMRDCJT9t .Lippe, P., Veeling, B., Perdikaris, P., Turner, R. E., and Brandstetter, J. Pde-refiner: Achieving accurate long roll-outs with neural PDE solvers. In Advances in Neural In-formation Processing Systems 36: Annual Conference on Neural Information Processing Systems 2023, NeurIPS 2023, New Orleans, LA, USA, December 10 - 16, 2023 ,2023. URL http://papers.nips.cc/paper_f iles/paper/2023/hash/d529b943af3dba7 34f8a7d49efcb6d09-Abstract-Conference. html .Liu, X., Gong, C., and Liu, Q. Flow straight and fast: Learning to generate and transfer data with rectified flow. In The Eleventh International Conference on Learning Representations, ICLR 2023, Kigali, Rwanda, May 1-5, 2023 , 2023. URL https://openreview.net/f orum?id=XVjTT1nw5z .Lu, C., Zhou, Y., Bao, F., Chen, J., Li, C., and Zhu, J. Dpm-solver: A fast ODE solver for diffusion prob-abilistic model sampling in around 10 steps. In Ad-vances in Neural Information Processing Systems 35: Annual Conference on Neural Information Processing Systems 2022, NeurIPS 2022, New Orleans, LA, USA, November 28 - December 9, 2022 , 2022. URL http: //papers.nips.cc/paper_files/paper/2 022/hash/260a14acce2a89dad36adc8eefe 7c59e-Abstract-Conference.html .Lu, L., Jin, P., Pang, G., Zhang, Z., and Karniadakis, G. E. Learning nonlinear operators via DeepONet based on the universal approximation theorem of operators. Nature Machine Intelligence , 3(3):218–229, 2021. doi: 10.103 8/s42256-021-00302-5. URL http://dx.doi.org /10.1038/s42256-021-00302-5 .Price, I., Sanchez-Gonzalez, A., Alet, F., Andersson, T. R., El-Kadi, A., Masters, D., Ewalds, T., Stott, J., Mohamed, S., Battaglia, P., Lam, R., and Willson, M. Probabilistic weather forecasting with machine learning. Nature , 637 (8044):84–90, 2025. doi: 10.1038/s41586-024-08252-9. URL http://dx.doi.org/10.1038/s4158 6-024-08252-9 .Salvi, C., Lemercier, M., and Gerasimovics, A. Neural stochastic pdes: Resolution-invariant learning of continu-ous spatiotemporal dynamics. In Advances in Neural In-formation Processing Systems 35: Annual Conference on Neural Information Processing Systems 2022, NeurIPS 2022, New Orleans, LA, USA, November 28 - December 9, 2022 , 2022. URL http://papers.nips.cc/p aper_files/paper/2022/hash/091166620 a04a289c555f411d8899049-Abstract-Con ference.html .Sauer, A., Lorenz, D., Blattmann, A., and Rombach, R. Adversarial diffusion distillation. In Computer Vi-sion - ECCV 2024 - 18th European Conference, Mi-lan, Italy, September 29-October 4, 2024, Proceedings, Part LXXXVI , volume 15144, pp. 87–103, 2024. doi: 10.1007/978-3-031-73016-0 \ 6. URL https://doi. org/10.1007/978-3-031-73016-0_6 .Shysheya, A., Diaconu, C., Bergamin, F., Perdikaris, P., Hern´ andez-Lobato, J. M., Turner, R. E., and Mathieu, E. On conditional diffusion models for PDE simulations. In 

Advances in Neural Information Processing Systems 37: Annual Conference on Neural Information Processing Systems 2024, NeurIPS 2024, Vancouver, BC, Canada, December 10 - 15, 2024 , 2024. URL http://papers .nips.cc/paper_files/paper/2024/hash 

12 StocBench: A Benchmark for Generative Modeling of Stochastic Dynamics 

/2974844555dc383ea16c5f35833c7a57-Abs tract-Conference.html .Song, J., Meng, C., and Ermon, S. Denoising diffusion im-plicit models. In 9th International Conference on Learn-ing Representations, ICLR 2021, Virtual Event, Austria, May 3-7, 2021 , 2021a. URL https://openreview .net/forum?id=St1giarCHLP .Song, Y. and Dhariwal, P. Improved techniques for train-ing consistency models. In The Twelfth International Conference on Learning Representations, ICLR 2024, Vi-enna, Austria, May 7-11, 2024 , 2024. URL https: //openreview.net/forum?id=WNzy9bRDvG .Song, Y., Sohl-Dickstein, J., Kingma, D. P., Kumar, A., Er-mon, S., and Poole, B. Score-based generative modeling through stochastic differential equations. In 9th Interna-tional Conference on Learning Representations, ICLR 2021, Virtual Event, Austria, May 3-7, 2021 , 2021b. URL 

https://openreview.net/forum?id=PxTI G12RRHS .Song, Y., Dhariwal, P., Chen, M., and Sutskever, I. Con-sistency models. In International Conference on Ma-chine Learning, ICML 2023, 23-29 July 2023, Honolulu, Hawaii, USA , volume 202, pp. 32211–32252, 2023. URL 

https://proceedings.mlr.press/v202/s ong23a.html .Srikishan, B., Santos, J. E., Muralidhar, N., and Young, C. D. TRIE: an evaluation framework for stochastic PDE surrogates. CoRR , abs/2607.00196, 2026. doi: 10.48550 /ARXIV.2607.00196. URL https://doi.org/10 .48550/arXiv.2607.00196 .13 StocBench: A Benchmark for Generative Modeling of Stochastic Dynamics 

## A. Dataset Generation 

A.1. Stochastic Kolmogorov Flow Training. The training dataset consists of 1,500 trajectories, each containing 200 frames. We simulate the vorticity by integrating Equation (1) using a pseudo-spectral solver with Euler–Maruyama time stepping. Each simulation starts from zero vorticity and runs for 100 warm-up snapshot intervals to allow the state distribution to converge toward the invariant measure μ. Simulations are performed on a 256 × 256 grid and subsequently reduced to 64 × 64 through spatial averaging. All trajectories are normalized using the global mean and standard deviation to improve training stability. Table 1 summarizes the physical constants and simulation parameters. 

Testing. For one-step evaluation, we generate 48 conditioning states and, for each, an ensemble of 5,000 next-snapshot states. These ensembles are used to estimate the ground-truth conditional statistics (e.g., mean and standard deviation) against which we compare the model-generated ensemble statistics. For the invariant-measure evaluation, we generate 5,000 additional states as starting points for autoregressive rollouts. 

A.2. Deterministic Kolmogorov Flow Training. For the deterministic variant, we use the same Kolmogorov-flow dynamics as in Equation (1), but replace the Wiener forcing with a piecewise-constant forcing that is held fixed between consecutive snapshots. This forcing is provided to the model as conditioning information. Consequently, each training sample consists of the current vorticity field, the forcing applied during the prediction interval, and the target vorticity at the next snapshot. 

Testing. For one-step evaluation, we generate 48 conditioning states. Since the forcing over the snapshot interval is observed, the next-snapshot state is deterministic. Each test case therefore consists of a conditioning state, the applied forcing, and the corresponding next-snapshot state. We compare the model ensemble mean with this deterministic target state and its ensemble standard deviation with zero. For the invariant measure and long-term trajectory evaluations, we generate 5,000 trajectories, each consisting of 50 state–forcing pairs.          

> Table 1. Physical constants and simulation parameters for the stochastic and deterministic Kolmogorov-flow datasets. The SDE is integrated with solver time step h, while snapshots are recorded at intervals of ∆t. The interval ∆talso defines the model prediction horizon, i.e., the model predicts the snapshot at t+ ∆ tfrom the state at t.

Parameter Symbol Value 

Solver time step h 10 −4

Snapshot interval ∆t 0.5

Solver grid resolution N 256 

Model grid resolution bN 64 

Viscosity ν 10 −3

Linear drag coefficient α 0.1

Forcing amplitude ϵ 1.0

## B. Baseline Specifications 

B.1. Diffusion Models: DDIM, DPM-2, DDPM 

DDIM, DPM-2, and DDPM share the same continuous-time ϵθ -prediction model trained with a linear variance-preserving (VP) noise schedule parameterized by βmin and βmax (Table 2). The baselines differ only in the sampling procedure: • DDIM corresponds to the η = 0 setting of the DDIM sampler (Song et al., 2021a), which recovers the first-order exponential integrator for the probability-flow ODE. • DPM-2 uses the second-order exponential integrator DPM-Solver-2 (Lu et al., 2022). Unlike the original DPM-Solver, which places integration steps uniformly in log-SNR, we discretize uniformly in time. Appendix D shows that both discretizations perform comparably, with the uniform schedule yielding slightly lower standard deviation errors at low inference budgets. 14 StocBench: A Benchmark for Generative Modeling of Stochastic Dynamics 

• DDPM corresponds to the η = 1 setting, i.e., ancestral sampling (Ho et al., 2020). 

B.2. Flow Matching 

The velocity model vθ is trained on the conditional OT path with minimum noise level σmin (Table 2). For sampling, we place Euler steps uniformly in τ .

B.3. Stochastic Interpolation 

We use the training objective of Chen et al. (2024b) with the interpolation schedules ατ = στ = 1 − τ and βτ = τ 2

(Table 2). Unlike diffusion and flow matching, sampling is initialized from the conditioning state rather than from Gaussian noise, and the model learns the conditional drift as a function of both the conditioning state and the interpolated state. We place Euler–Maruyama steps uniformly in τ , restricting the integration to τ ∈ [0 , 0.999] to avoid the degenerate noise-free endpoint. 

B.4. Consistency Distillation 

Consistency distillation trains a student model from a pretrained EDM diffusion teacher using the Karras noise schedule σ

(Karras et al., 2022). The student is initialized with the teacher weights and trained to make consistent predictions, such that neighboring points along the teacher’s probability-flow ODE trajectory are mapped to the same clean sample. To this end, we discretize the teacher’s noise schedule into T = 100 noise levels. For training stability, we use an exponential moving average (EMA) target in the self-consistency objective. Sampling can either use a single step at the highest noise level or multiple steps at progressively lower noise levels. CD-1 corresponds to single-step sampling using only the highest noise level σ(99) . For multi-step sampling, we select the noise levels based on the schedule analysis in Appendix C. In CD-2, the initial prediction at σ(99) is refined by a second evaluation at the noise level that is most effective for the specific task: we use σ(97) for the stochastic task and σ(50) for the deterministic task. 

B.5. Adversarial Diffusion Distillation 

ADD combines a frozen teacher, a trainable student generator, and a discriminator. In our implementation, the teacher and generator are flow matching models. The generator learns to match the 10-step teacher prediction in a single step while producing samples the discriminator cannot distinguish from real data. The discriminator uses the encoder and the bottleneck of the teacher U-Net as a feature extractor. MLP heads are attached to the input layer, each encoder stage, and the bottleneck. Each head applies global average pooling to its feature maps and outputs a scalar logit. The final discriminator score is obtained by summing the hinge losses of all logits. The discriminator is trained to assign higher scores to real samples than to generated samples and is regularized by an R1 gradient penalty (coefficient γ). The generator objective combines a distillation term and an adversarial term. The distillation term causes the generator to match the teacher model predictions, while the adversarial term forces the generator to produce realistic samples that receive high discriminator scores. The weight λ balances the adversarial and distillation terms, with λ = 1 corresponding to pure distillation and λ = 0 to a standard GAN. The generator and discriminator are trained in parallel, with the generator updated once every rgen = 5 discriminator updates to keep the discriminator ahead and provide a stable training signal. We additionally use a balancing mechanism similar to Berthelot et al. (2017). 

## C. Consistency Distillation Sampling Schedules 

Consistency models support multi-step sampling by re-evaluating the consistency function at progressively lower noise levels. Unlike diffusion- and flow-based samplers, this procedure does not arise from discretizing an underlying SDE or probability-flow ODE. As a consequence, more sampling steps do not necessarily improve sample quality; Chen et al. (2025) show that too many steps can even degrade it. This motivates an empirical analysis of sampling schedules for the stochastic and deterministic settings. 15 StocBench: A Benchmark for Generative Modeling of Stochastic Dynamics 

Table 2. Model-specific hyperparameters. The diffusion models share the same continuous-time ϵθ -prediction parameterization and differ only in the sampler used at inference time. 

Parameter Symbol Value Diffusion 

Variance schedule β(τ ) VP linear Schedule limits [βmin , β max ] [0 .1, 20] 

Sampler – DPM-2/DDIM/DDPM 

Flow Matching 

Minimum noise level σmin 10 −3

Integrator – Euler 

Stochastic Interpolation 

Conditioning coefficient ατ 1 − τ

Target coefficient βτ τ 2

Noise schedule στ 1 − τ

Time range τ [0 , 0.999] 

Integrator – Euler–Maruyama 

Parameter Symbol Value Consistency Distillation 

Noise schedule σ Karras Noise schedule exponent ρ 7

Minimum noise level σmin 2 × 10 −3

Maximum noise level σmax 80 

Noise levels T 100 

EMA decay – 0.999 

Adversarial Diffusion Distillation 

Teacher steps Tteacher 10 

Student steps Tstudent 1

Update ratio rgen 5

Distillation weight λ 0.9

R1 coefficient γ 1.0

Figure 8 compares different sampling schedules, reporting the mean and standard deviation errors after each denoising step. In the stochastic case, a single step at the highest noise level already performs near optimally. A second step at the high-noise end slightly improves the mean error, while further steps yield no benefit. Schedules that distribute many steps across the full noise range perform worse on both metrics. In the deterministic case, a single step at the highest noise level is insufficient: steps at lower noise levels are needed to obtain an accurate mean and eliminate residual variability. Here, schedules with evaluations distributed uniformly across the noise range are most effective. 

> 0.03 0.04
> Std
> 020 40 60 80
> Noise level index
> 0.10 0.15
> Mean

(a) Stochastic 

> 2468
> Std
> 020 40 60 80
> Noise level index
> 0.05 0.10
> Mean

(b) Deterministic 

Figure 8. Evaluation of different Consistency Distillation sampling schedules for stochastic and deterministic Kolmogorov flow. Each curve corresponds to a sampling schedule. The mean and standard deviation errors at each marker are obtained by performing denoising steps at all marked noise levels up to and including that marker. 

## D. DPM-2 Discretization Schedule 

The original DPM-Solver (Lu et al., 2022) places integration steps uniformly in log-SNR, which concentrates evaluations in the low-noise regime. Our DPM-2 baseline instead discretizes uniformly in time. Figures 9a and 9b compare the two discretizations. The mean errors are nearly identical on both tasks. In the stochastic case, the standard deviation errors converge at high budgets, while at low budgets the log-SNR schedule amplifies the overestimation of the standard deviation (cf. Figure 5, left). In the deterministic case, the uniform schedule has lower residual variability across all inference budgets. 16 StocBench: A Benchmark for Generative Modeling of Stochastic Dynamics         

> 050 100 150 200 250 300 350 400

NFE 

> 0.00 0.02 0.04 0.06 0.08 0.10
> Relative L2 error (mean)

(a)         

> 050 100 150 200 250 300 350 400

NFE 

> 0.00 0.02 0.04 0.06 0.08 0.10
> Relative L2 error (std)

(b)  

> DPM-2 (uniform) DPM-2 (logSNR)

(a) Stochastic         

> 050 100 150 200 250 300 350 400

NFE 

> 0.0200 0.0205 0.0210 0.0215 0.0220 0.0225 0.0230 0.0235 0.0240
> Relative L2 error (mean)

(a)         

> 050 100 150 200 250 300 350 400

NFE 

> 0.00 0.25 0.50 0.75 1.00 1.25 1.50
> L2 error (std)

(b)  

> DPM-2 (uniform) DPM-2 (logSNR)

(b) Deterministic 

Figure 9. Uniform vs. log-SNR discretization for DPM-2 on stochastic and deterministic Kolmogorov flow. 

17 StocBench: A Benchmark for Generative Modeling of Stochastic Dynamics 

## E. Supplementary Evaluation Figures  

> 10 010 1

Wavenumber k   

> 10 −2
> 10 −1
> 10 0
> 10 1
> 10 2
> 10 3
> Enstrophy
> 6×10 0
> SI DPM-2 DDIM DDPM FM CD-1 CD-2 ADD-FM Ground truth

Figure 10. Ensemble-averaged radial enstrophy spectra after 50 autoregressive rollout steps on stochastic Kolmogorov flow. Dotted, dashed, and solid lines indicate models using 1, 2, and 400 NFEs, respectively.  

> 10 010 1

Wavenumber k   

> 10 −2
> 10 −1
> 10 0
> 10 1
> 10 2
> 10 3
> Enstrophy
> 6×10 0
> SI DPM-2 DDIM DDPM FM CD-1 CD-2 ADD-FM Ground truth

Figure 11. Ensemble-averaged radial enstrophy spectra after 50 autoregressive rollout steps on deterministic Kolmogorov flow. Dotted, dashed, and solid lines indicate models using 1, 2, and 400 NFEs, respectively. 

18 StocBench: A Benchmark for Generative Modeling of Stochastic Dynamics 

> (a) Ground truth

Mean, seed 110 Mean, seed 111 Mean, seed 112 Std, seed 110 Std, seed 111 Std, seed 112                         

> (b) SI (c) DPM-2 (d) DDIM (e) DDPM (f) FM (g) CD-1 (h) CD-2 (i) ADD-FM
> −202
> −0.25 0.00 0.25
> −202
> −0.20.00.2
> −202
> −0.20.00.2
> 0.40.60.8
> −0.20.00.2
> 0.40.60.8
> −0.10.00.1
> 0.40.50.6
> −0.05 0.00 0.05

Figure 12. Conditional means and standard deviations for three stochastic Kolmogorov flow conditions. The top row presents the ground-truth statistics, and the remaining rows show model errors relative to the ground truth. Color scales are shared within each column. 

19