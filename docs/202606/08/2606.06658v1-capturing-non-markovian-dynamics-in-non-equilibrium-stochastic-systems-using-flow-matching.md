---
title: Capturing non-Markovian dynamics in non-equilibrium stochastic systems using flow matching
title_zh: 使用流匹配捕捉非平衡随机系统中的非马尔可夫动力学
authors: "Bhargav Sriram Siddani, John B. Bell, Alejandro L. Garcia, Ishan Srivastava"
date: 2026-06-04
pdf: "https://arxiv.org/pdf/2606.06658v1"
tags: ["query:genmod-fluid"]
score: 8.0
evidence: 使用流匹配方法模拟类似流体动力学的随机粒子系统
tldr: 粗粒化随机偏微分方程如正则化Dean-Kawasaki方程无法准确捕捉非马尔可夫效应主导的短时间动力学和低粒子密度下的非高斯分布。本文提出一种基于生成流匹配的方法，直接对粒子模拟中通量的概率分布进行建模，显式纳入非马尔可夫与非高斯效应。在非相互作用布朗粒子体系的Kramers首次通过时间问题中，该方法准确刻画了短时间行为，并在数密度统计矩预测上优于马尔可夫基线模型，为复杂随机系统的粗粒化建模提供了新范式。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.06658v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1447, \"height\": 785, \"label\": \"Figure\"}]"
motivation: 现有粗粒化随机偏微分方程无法准确描述短时间非马尔可夫动力学和低密度非高斯分布，需要新方法捕捉这些效应。
method: 提出生成流匹配方法，直接建模粒子模拟中通量的概率分布，显式纳入非马尔可夫和非高斯效应。
result: 在Kramers首次通过时间问题中，模型准确捕捉短时间行为，数密度统计矩预测优于正则化Dean-Kawasaki方程。
conclusion: 该方法有效克服了马尔可夫近似在短时间和低密度场合的局限，为随机系统粗粒化提供了更精确的工具。
---

## 摘要
由粗粒化随机偏微分方程（SPDE）表示的随机粒子系统的流体动力学模型，例如正则化的Dean-Kawasaki（DK）方程，无法准确捕捉主要由非马尔可夫效应主导的短时系统动力学，以及分布高度非高斯化的低粒子密度区域。我们开发了一种生成式流匹配方法，该方法直接从粒子模拟中对概率分布进行建模，并明确纳入非马尔可夫和非高斯效应。作为演示，我们使用该方法模拟了非相互作用布朗粒子系统的Kramers首次通过时间问题。我们表明，该模型准确捕捉了短时行为，并且与马尔可夫基线（正则化DK方程）的解相比，提供了对数量密度统计矩更好的预测。

## Abstract
Hydrodynamic models of stochastic particle systems represented by coarse-grained stochastic partial differential equations (SPDE), such as the regularized Dean-Kawasaki (DK) equation, do not accurately capture the short-time system dynamics that is dominated by non-Markovian effects, and low particle density regimes where the distributions are highly non-Gaussian. We develop a generative flow matching method that directly models the probability distribution of fluxes from particle simulations that explicitly incorporates non-Markovian and non-Gaussian effects. As a demonstration, we use this method to simulate the Kramers first passage time problem for a system of non-interacting Brownian particles. We show the model accurately captures the short-time behavior and provides better predictions of the statistical moments of the number density when compared against the solution of the Markovian baseline, regularized DK equation.

---

## 论文详细总结（自动生成）

### 论文核心问题与整体含义（研究动机和背景）

- **核心问题**：粗粒化随机偏微分方程（如正则化Dean-Kawasaki方程）在以下两种情况下失效：
  1. 短时间动力学受非马尔可夫效应（记忆效应）主导；
  2. 低粒子密度区域，概率分布高度非高斯化。
- **研究动机**：经典Mori-Zwanzig理论表明粗粒化会引入记忆项，但正则化DK方程将其忽略；需要新方法显式捕获这些效应以提高短时和低密度的预测精度。
- **整体含义**：提出一种基于生成流匹配的框架，直接对粒子模拟中通量的概率分布建模，将非马尔可夫与非高斯性纳入粗粒化模型，为随机粒子系统的精确模拟提供新范式。

### 论文提出的方法论

#### 核心思想
- 将粒子模拟中跨网格面通量 \(J(x,t)\) 的概率分布视为条件分布，依赖于历史状态（过去 \(k\) 步的左右粒子数 \(N_L^k, N_R^k\) 和通量 \(J^k\)），而非仅依赖当前密度。
- 利用**流匹配（Flow Matching）**生成模型学习该条件分布，再以生成的随机通量替代正则化DK方程中的高斯白噪声通量，从而模拟SPDE。

#### 关键技术细节
- **问题定义**：定义通量 \(J\)（净粒子数穿过面），其概率密度函数 \(p(J|N_L^k, N_R^k, J^k)\) 由流匹配学习。
- **流匹配框架**：
  - 构建从源分布 \(s\)（Student t-分布）到目标分布 \(r\)（真实通量分布）的概率路径 \(p_\tau\)（\(\tau \in [0,1]\)）。
  - 训练速度场神经网络 \(u^\theta_\tau\)，通过条件流匹配损失：
    \[
    L_{\text{CFM}}(\theta) = \mathbb{E}_{\tau, Z_0, Z_1} \| u^\theta_\tau(Z_\tau) - (\dot{a}_\tau Z_0 + \dot{b}_\tau Z_1) \|^2,
    \]
    其中 \(Z_\tau = a_\tau Z_0 + b_\tau Z_1\)，\(a_\tau = \cos(\frac{1}{2}\pi\tau), b_\tau = \sin(\frac{1}{2}\pi\tau)\)。
  - 利用 **统计反射对称性** 简化训练：\(p(J|a,b) = p(-J|b,a)\)，通过构造对称速度场强制该性质。
- **网络架构**：
  - 无历史（\(k=0\)）：使用 **DeepONet**；
  - 有历史（\(k>0\)）：使用 **Transformer**，输入包括历史序列 \(N_L^k, N_R^k, J^k\) 和当前 \(\tau\) 的噪声 \(Z_\tau\)。
- **训练数据生成**：来自一维平衡态布朗粒子的模拟，平均每网格粒子数1~50，历史长度 \(k\) 均匀采样0~10。

#### 算法流程（文字说明）
1. 准备训练数据：从粒子模拟中收集条件对 \((N_L^k, N_R^k, J^k, J_{\text{target}})\)。
2. 训练流匹配模型：最小化条件流匹配损失，获得速度场 \(u^\theta_\tau\)。
3. 在SPDE模拟中每时间步：
   - 给定当前系统状态，提取各面历史信息；
   - 从源分布（Student t）采样初始噪声 \(Z_0\)；
   - 使用训练后的速度场求解常微分方程（从 \(\tau=0\) 到 \(\tau=1\)，均匀20步欧拉方法）得到样本 \(Z_1\)（即当前时刻的随机通量 \(\tilde{F}^B\)）；
   - 结合确定性部分 \(\bar{F}^B\) 和势通量 \(F^V\)，更新密度场。

### 实验设计

- **基准问题**：Kramers首次通过时间问题的变体，一维系统，外部势阱 \(V(x) = (x-0.3)^2(x-0.7)^2\)，初始粒子集中于左阱（\(0.2 \le x \le 0.4\)），观察粒子向右阱（\(x=0.7\)附近）穿越能量势垒（\(x=0.5\)）的统计。
- **场景参数**：
  - 网格数：100个有限体积单元（\(\Delta x = 1 \times 10^{-2}\)）；
  - 时间步长：\(\Delta t = 3 \times 10^{-6}\)；
  - 总粒子数：\(10^3\)个非相互作用布朗粒子；
  - 模拟步数：约 \(3 \times 10^3\) 步；
  - 独立实现数：5120个。
- **对比方法**：
  1. **布朗随机游走**（ground truth）；
  2. **正则化Dean-Kawasaki（DK）模型**（马尔可夫基线）；
  3. **流匹配模型（无历史，\(k=0\)）**（马尔可夫版）；
  4. **流匹配模型（有历史，\(k=10\)）**（非马尔可夫版）。
- **评估指标**：目标区域（\(x=\beta\)附近）粒子数的**空间分布**、**平均密度**、**高阶统计矩**（如方差、偏度等）。

### 资源与算力

- 论文未明确说明GPU型号、数量或训练时长。
- 提及：**所有模拟在CPU上运行**，非马尔可夫FM模型在100个网格/CPU的规模下“平均每个时间步9秒”，并称其扩展性良好。
- 训练数据生成和模型训练的具体硬件资源未披露。

### 实验数量与充分性

- **实验数量**：一组主要实验（Kramers问题），包含5120个独立实现，对比4种方法，展示短时早期结果（约 \(t=5.1\times10^{-3}\)）。
- **充分性评估**：
  - **正面**：5120个样本足够统计稳定；采用多种模型对比，且区分了有无历史记忆；可视化展示独立实现和统计矩演化，直观可信。
  - **不足**：
    - 仅测试了一种势阱和一种初始条件，未在不同势函数、不同相互作用类型或更高维度上验证泛化性；
    - 未对历史长度 \(k\) 进行系统消融（仅测试 \(k=0\) 和 \(k=10\)）；
    - 仅在短时（早期）展示优势，未系统分析长期偏差；
    - 未与其他机器学习方法（如RNN、LSTM、扩散模型）进行公平对比。

### 论文的主要结论与发现

1. 正则化DK方程在短时会产生大量非物理负密度（图1白色区域），而流匹配模型（尤其有记忆的）显著减少此问题。
2. 非马尔可夫FM模型（\(k=10\)）比马尔可夫FM模型（\(k=0\)）和DK方程**更准确地捕捉短时空间粒子分布**。
3. 高阶统计矩（如方差等）的预测上，非马尔可夫FM模型在早期明显优于基线；但**随时间推移（后期），预测偏差逐渐增大**。
4. 记忆效应在短时动力学中至关重要，流匹配框架可有效纳入该效应，为复杂随机系统粗粒化提供更精确的工具。

### 优点

- **创新性**：首次将生成流匹配用于粗粒化SPDE中的通量分布建模，而非直接学习密度演化。
- **物理一致性**：显式引入统计反射对称性，减少模型容量需求并强化物理约束。
- **灵活性**：支持任意历史长度 \(k\)，可捕捉非马尔可夫性；采用不同网络架构适配有无记忆情形。
- **实用价值**：方法适用于短时关键场景（如化学反应、单列扩散），并有望在粒子间长程相互作用的复杂系统中降低计算成本（对比直接粒子模拟）。

### 不足与局限

- **实验覆盖有限**：仅一个数值案例（非相互作用布朗粒子、特定势阱），缺乏在相互作用粒子、高维系统、不同噪声强度下的验证。
- **长期预测偏差**：论文明确指出后期误差随时间增加，说明当前模型在长时间尺度上缺乏稳定性或记忆机制不够完善。
- **计算效率**：虽然声称对长程相互作用有优势，但当前实现每个时间步需解20步ODE采样，且历史长度为10时使用Transformer，每网格每时间步9秒（CPU）；若系统很大，可能仍较昂贵。
- **训练数据依赖**：需要从粒子模拟生成大量条件样本，数据收集成本未量化；对于复杂系统可能本身就很昂贵。
- **未与多种机器学习基线对比**：如扩散模型、VAE、GAN等，也未比较不同历史长度的影响。
- **缺乏严格的误差分析**：未给出定量误差（如相对误差、置信区间），仅图示定性比较。

（完）
